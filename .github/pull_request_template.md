## Checklist

- [ ] I checked whether new or changed RPCs need `qdrant.cloud.event.v1.event_options` for audit/event publishing.
- [ ] If `event_options` is used, I checked whether `additional_context_fields` or `additional_payload_fields` are needed and kept them minimal.
