# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/metering/v1/metering.proto
"""buf.validate rules for qdrant/cloud/metering/v1/metering.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.metering.v1.metering_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from typing import Annotated

from annotated_types import Ge, Le, MaxLen, MinLen

from google.protobuf import timestamp_pb2 as _google_protobuf_timestamp_pb2

# ListMonthlyMeteringsRequest is the request for the ListMonthlyMeterings function
ListMonthlyMeteringsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]

# ListMonthlyMeterings is the response from the ListMonthlyMeterings function

# ListMeteringsRequest is the request for the ListMeterings function
ListMeteringsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListMeteringsRequestYear = Annotated[
    int,
    Ge(2000),
    Le(3000),
]
ListMeteringsRequestMonth = Annotated[
    int,
    Ge(1),
    Le(12),
]

# ListMeteringsResponse is the response from the ListMeterings function

# MonthlyMeteringSummary represents a single month's metering summary.
MonthlyMeteringSummaryYear = Annotated[
    int,
    Ge(2000),
    Le(3000),
]
MonthlyMeteringSummaryMonth = Annotated[
    int,
    Ge(1),
    Le(12),
]
MonthlyMeteringSummaryAmountMillicents = Annotated[
    int,
    Ge(0),
]
MonthlyMeteringSummaryCurrency = Annotated[
    str,
    MinLen(3),
    MaxLen(3),
    "string.pattern = ^[A-Z]{3}$",
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS Metering represents a detailed metering item.
# cel[metering_item.end_time_gt_start_time]: this.end_time > this.start_time
#   message: end_time must be after start_time
#   refs: end_time, start_time, this
#   calls: _>_
MeteringItemAccountId = Annotated[
    str,
    "string.uuid = true",
]
MeteringItemClusterId = Annotated[
    str,
    "string.uuid = true",
]
MeteringItemClusterName = Annotated[
    str,
    MinLen(1),
]
MeteringItemStartTime = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
MeteringItemEndTime = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
MeteringItemBillableEntityId = Annotated[
    str,
    "string.uuid = true",
]
MeteringItemBillableEntityReferenceName = Annotated[
    str,
    MinLen(1),
]
MeteringItemBillableEntityType = Annotated[
    str,
    MinLen(1),
]
MeteringItemPricePerHour = Annotated[
    int,
    Ge(0),
]
MeteringItemUsageHours = Annotated[
    float,
    Ge(0),
]
MeteringItemAmountMillicents = Annotated[
    int,
    Ge(0),
]
MeteringItemDiscountAmountMillicents = Annotated[
    int,
    Ge(0),
]
MeteringItemDiscountAmountPercent = Annotated[
    float,
    Ge(0),
    Le(100),
]
MeteringItemCurrency = Annotated[
    str,
    MinLen(3),
    MaxLen(3),
    "string.pattern = ^[A-Z]{3}$",
]

# GetUsageBreakdownRequest is the request for the GetUsageBreakdown function
# cel[get_usage_breakdown_request.end_time_gt_start_time]: this.end_time > this.start_time
#   message: end_time must be after start_time
#   refs: end_time, start_time, this
#   calls: _>_
GetUsageBreakdownRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetUsageBreakdownRequestStartTime = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
GetUsageBreakdownRequestEndTime = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]

# GetUsageBreakdownResponse is the response from the GetUsageBreakdown function

# UsageBreakdownMonth groups usage for a single calendar month.
UsageBreakdownMonthYear = Annotated[
    int,
    Ge(2000),
    Le(3000),
]
UsageBreakdownMonthMonth = Annotated[
    int,
    Ge(1),
    Le(12),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS UsageBreakdownFunding states how much of a period's usage prepaid credits paid for.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS CreditDrawdown is the amount one credit contract paid towards a usage period.
CreditDrawdownCreditContractId = Annotated[
    str,
    MinLen(1),
]
CreditDrawdownAmountMillicents = Annotated[
    int,
    Ge(0),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS UsageBreakdownAccount groups per-cluster usage for a single account in a given month.
UsageBreakdownAccountAccountId = Annotated[
    str,
    "string.uuid = true",
]
UsageBreakdownAccountAccountName = Annotated[
    str,
    MinLen(1),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS ClusterUsageConfig describes the shape and placement of a cluster for the cluster-usage billable metric.
ClusterUsageConfigCpuVcpu = Annotated[
    int,
    Ge(0),
]
ClusterUsageConfigRamGib = Annotated[
    int,
    Ge(0),
]
ClusterUsageConfigDiskGib = Annotated[
    int,
    Ge(0),
]
ClusterUsageConfigNodeCount = Annotated[
    int,
    Ge(0),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS ClusterExtraDiskConfig describes placement and tier for the cluster-extra-disk billable metric.
ClusterExtraDiskConfigExtraDiskGib = Annotated[
    int,
    Ge(0),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS ClusterStorageTierConfig describes placement and storage tier for the cluster-storage-tier billable metric.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS BackupStorageConfig describes placement and tier for the backup-storage billable metric.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS InferenceConfig describes placement, model and tier for the inference-tokens billable metric.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS UsageBreakdownCluster represents the usage for a single cluster within a month.
# oneof config: exactly one of cluster_usage_config, cluster_extra_disk_config, cluster_storage_tier_config, backup_storage_config, inference_config
# cel[usage_breakdown_cluster.end_time_gt_start_time]: this.end_time > this.start_time
#   message: end_time must be after start_time
#   refs: end_time, start_time, this
#   calls: _>_
UsageBreakdownClusterClusterId = Annotated[
    str,
    "string.uuid = true",
]
UsageBreakdownClusterClusterName = Annotated[
    str,
    MinLen(1),
]
UsageBreakdownClusterStartTime = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
UsageBreakdownClusterEndTime = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
UsageBreakdownClusterAmountMillicents = Annotated[
    int,
    Ge(0),
]
UsageBreakdownClusterCurrency = Annotated[
    str,
    MinLen(3),
    MaxLen(3),
    "string.pattern = ^[A-Z]{3}$",
]
UsageBreakdownClusterBillableEntityType = Annotated[
    str,
    MinLen(1),
]
UsageBreakdownClusterQuantity = Annotated[
    float,
    Ge(0),
]
UsageBreakdownClusterDiscountAmountMillicents = Annotated[
    int,
    Ge(0),
]
UsageBreakdownClusterDiscountAmountPercent = Annotated[
    float,
    Ge(0),
    Le(100),
]

