# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/monitoring/v1/monitoring.proto
"""buf.validate rules for qdrant/cloud/monitoring/v1/monitoring.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.monitoring.v1.monitoring_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from typing import Annotated

from annotated_types import Ge, Le, MinLen

from google.protobuf import duration_pb2 as _google_protobuf_duration_pb2
from google.protobuf import timestamp_pb2 as _google_protobuf_timestamp_pb2
from qdrant.cloud.monitoring.v1.monitoring_pb2 import Aggregator, ClusterAlertSeverity, ClusterAlertState, ClusterAlertType, ClusterMetricOverview, InferenceMetricsInterval, ResourceValue

# Aggregator defines how metrics should be aggregated over time.
# Without AGGREGATOR_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
AggregatorStrict = Annotated[
    Aggregator,
    Ge(1),
]

# InferenceMetricsInterval defines the histogram bucket size for inference metrics.
# Without INFERENCE_METRICS_INTERVAL_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
InferenceMetricsIntervalStrict = Annotated[
    InferenceMetricsInterval,
    Ge(1),
]

# ClusterAlertState mirrors the backend state of a cluster alert. It is used both as the optional request-side filter AND echoed per row on the ClusterAlert response.
# Without CLUSTER_ALERT_STATE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
ClusterAlertStateStrict = Annotated[
    ClusterAlertState,
    Ge(1),
]

# ClusterAlertType is the category of a cluster alert. Mirrors the backend alert types.
# Without CLUSTER_ALERT_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
ClusterAlertTypeStrict = Annotated[
    ClusterAlertType,
    Ge(1),
]

# ClusterAlertSeverity is the tier used by the UI to render badge color and announce severity to assistive tech. Color alone never conveys severity - clients must render the label.
# Without CLUSTER_ALERT_SEVERITY_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
ClusterAlertSeverityStrict = Annotated[
    ClusterAlertSeverity,
    Ge(1),
]

# GetClusterSummaryMetricsRequest is the request for the GetClusterSummaryMetrics function
GetClusterSummaryMetricsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterSummaryMetricsRequestClusterId = Annotated[
    str,
    "string.uuid = true",
]

# GetClusterSummaryMetricsResponse is the response from the GetClusterSummaryMetrics function

# GetClusterUsageMetricsRequest is the request for the GetClusterUsageMetrics function
# cel[get_cluster_usage_metrics.until_gt_since]: !has(this.since) || !has(this.until) || this.until > this.since
#   message: until must be after since
#   refs: since, this, until
#   calls: !_, _>_, _||_
GetClusterUsageMetricsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterUsageMetricsRequestClusterId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterUsageMetricsRequestAggregator = Annotated[
    AggregatorStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]

# GetClusterUsageMetricsResponse is the response from the GetClusterUsageMetrics function

# GetClusterLogsRequest is the request for the GetClusterLogs function
# cel[get_cluster_logs.until_gt_since]: !has(this.since) || !has(this.until) || this.until > this.since
#   message: until must be after since
#   refs: since, this, until
#   calls: !_, _>_, _||_
GetClusterLogsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterLogsRequestClusterId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterLogsRequestLimit = Annotated[
    int,
    Ge(1),
    Le(10000),
]

# GetClusterLogsResponse is the response from the GetClusterLogs function

# GetClusterEventsRequest is the request for the GetClusterEvents function
# cel[get_cluster_events.until_gt_since]: !has(this.since) || !has(this.until) || this.until > this.since
#   message: until must be after since
#   refs: since, this, until
#   calls: !_, _>_, _||_
GetClusterEventsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterEventsRequestClusterId = Annotated[
    str,
    "string.uuid = true",
]

# GetClusterEventsRequest is the response from the GetClusterEvents function

# GetClusterInferenceMetricsRequest is the request for the GetClusterInferenceMetrics function
# cel[get_cluster_inference_metrics.until_gt_since]: !has(this.since) || !has(this.until) || this.until > this.since
#   message: until must be after since
#   refs: since, this, until
#   calls: !_, _>_, _||_
GetClusterInferenceMetricsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterInferenceMetricsRequestClusterId = Annotated[
    str,
    "string.uuid = true",
]
GetClusterInferenceMetricsRequestInterval = Annotated[
    InferenceMetricsIntervalStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
GetClusterInferenceMetricsRequestInferenceModelId = Annotated[
    str,
    MinLen(1),
]

# GetClusterInferenceMetricsResponse is the response from the GetClusterInferenceMetrics function

# ClusterInferenceModelMetrics groups histogram data for a single inference model.
ClusterInferenceModelMetricsInferenceModelId = Annotated[
    str,
    MinLen(1),
]

# ClusterNodeMetrics contains metric overview for a node.
ClusterNodeMetricsNodeId = Annotated[
    str,
    MinLen(1),
]
ClusterNodeMetricsCpu = Annotated[
    ClusterMetricOverview,
    "required",
]
ClusterNodeMetricsRam = Annotated[
    ClusterMetricOverview,
    "required",
]
ClusterNodeMetricsRamCache = Annotated[
    ClusterMetricOverview,
    "required",
]
ClusterNodeMetricsRamRss = Annotated[
    ClusterMetricOverview,
    "required",
]
ClusterNodeMetricsRamQdrantRss = Annotated[
    ClusterMetricOverview,
    "required",
]
ClusterNodeMetricsDisk = Annotated[
    ClusterMetricOverview,
    "required",
]

# ClusterMetricOverview contains both the average values of a metric over predefined intervals, and the total available value of that resource.
ClusterMetricOverviewTotal = Annotated[
    ResourceValue,
    "required",
]

# IntervalAverage represents the average value of a metric over a specific time interval.
IntervalAverageInterval = Annotated[
    _google_protobuf_duration_pb2.Duration,
    "required",
]
IntervalAverageValue = Annotated[
    float,
    Ge(0),
]

# ResourceValue represents the total value of a resource along with its unit.
ResourceValueValue = Annotated[
    float,
    Ge(0),
]
ResourceValueUnit = Annotated[
    str,
    MinLen(1),
]

# ClusterNodeUsageMetrics defines per-node usage metrics.
ClusterNodeUsageMetricsNodeId = Annotated[
    str,
    MinLen(1),
]

# A single metric data point.
MetricTimestamp = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
MetricValue = Annotated[
    float,
    Ge(0),
]

# LogEntry represents a single log line.
LogEntryTimestamp = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
LogEntryMessage = Annotated[
    str,
    MinLen(1),
]

# ListClusterAlertsRequest is the request for the ListClusterAlerts function
ListClusterAlertsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListClusterAlertsRequestClusterId = Annotated[
    str,
    "string.uuid = true",
]

# ListClusterAlertsResponse is the response from the ListClusterAlerts function

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS ClusterAlert is a single alert instance for a cluster.

