# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/serverless/platform/v1/platform.proto
"""buf.validate rules for qdrant/cloud/serverless/platform/v1/platform.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.serverless.platform.v1.platform_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from typing import Annotated

from annotated_types import MaxLen, MinLen

# ListCloudProvidersRequest is the request for the ListCloudProviders function.
ListCloudProvidersRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]

# ListCloudProvidersResponse is the response from the ListCloudProviders function.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_REQUEST_FIELDS ListGlobalCloudProvidersRequest is the request from the ListGlobalCloudProviders function.

# ListGlobalCloudProvidersResponse is the response from the ListGlobalCloudProviders function.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_REQUEST_FIELDS ListGlobalCloudProviderRegionsRequest is the request for the ListGlobalCloudProviderRegions function.
ListGlobalCloudProviderRegionsRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]

# ListGlobalCloudProviderRegionsResponse is the response from the ListGlobalCloudProviderRegions function.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_REQUEST_FIELDS GetGlobalCloudProviderRegionRequest is the request for the GetGlobalCloudProviderRegion function.
GetGlobalCloudProviderRegionRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
GetGlobalCloudProviderRegionRequestRegionId = Annotated[
    str,
    MinLen(1),
]

# GetGlobalCloudProviderRegionResponse is the response from the GetGlobalCloudProviderRegion function.

# ListCloudProviderRegionsRequest is the request for the ListCloudProviderRegions function.
ListCloudProviderRegionsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListCloudProviderRegionsRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]

# ListCloudProviderRegionsResponse is the response from the ListCloudProviderRegions function.

# GetCloudProviderRegionRequest is the request for the GetCloudProviderRegion function.
GetCloudProviderRegionRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetCloudProviderRegionRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
GetCloudProviderRegionRequestRegionId = Annotated[
    str,
    MinLen(1),
]

# GetCloudProviderRegionResponse is the response from the GetCloudProviderRegion function.

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS CloudProvider represents a cloud provider identifier and name.
CloudProviderId = Annotated[
    str,
    MinLen(3),
]
CloudProviderName = Annotated[
    str,
    MinLen(1),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS CloudProviderRegion represents a cloud provider region.
CloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]
CloudProviderRegionName = Annotated[
    str,
    MinLen(1),
]
CloudProviderRegionProvider = Annotated[
    str,
    MinLen(1),
]
CloudProviderRegionCountryIsoCode = Annotated[
    str,
    MinLen(2),
    MaxLen(2),
    "string.pattern = ^[A-Z]{2}$",
]
CloudProviderRegionGeographicalSubRegion = Annotated[
    str,
    MinLen(1),
]

