# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/serverless/monitoring/v1/monitoring.proto
"""buf.validate rules for qdrant/cloud/serverless/monitoring/v1/monitoring.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.serverless.monitoring.v1.monitoring_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from typing import Annotated

from annotated_types import Ge, Gt, Le, MaxLen, MinLen

from google.protobuf import duration_pb2 as _google_protobuf_duration_pb2
from google.protobuf import timestamp_pb2 as _google_protobuf_timestamp_pb2
from qdrant.cloud.serverless.monitoring.v1.monitoring_pb2 import Aggregator, InferenceMetricsInterval, SpaceAlertSeverity, SpaceAlertState, SpaceAlertType, SpaceMetricOverview, SpaceQuotaSnapshot

# Aggregator defines how metrics should be aggregated over time.
# Without AGGREGATOR_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
AggregatorStrict = Annotated[
    Aggregator,
    Ge(1),
]

# InferenceMetricsInterval defines the histogram bucket size for inference metrics.
# Without INFERENCE_METRICS_INTERVAL_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
InferenceMetricsIntervalStrict = Annotated[
    InferenceMetricsInterval,
    Ge(1),
]

# SpaceAlertState mirrors the backend state of a space alert. It is used both as the optional request-side filter AND echoed per row on the SpaceAlert response.
# Without SPACE_ALERT_STATE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
SpaceAlertStateStrict = Annotated[
    SpaceAlertState,
    Ge(1),
]

# SpaceAlertType is the category of a space alert. Mirrors the backend alert types.
# Without SPACE_ALERT_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
SpaceAlertTypeStrict = Annotated[
    SpaceAlertType,
    Ge(1),
]

# SpaceAlertSeverity is the tier used by the UI to render badge color and announce severity to assistive tech. Color alone never conveys severity - clients must render the label.
# Without SPACE_ALERT_SEVERITY_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
SpaceAlertSeverityStrict = Annotated[
    SpaceAlertSeverity,
    Ge(1),
]

# GetSpaceSummaryMetricsRequest is the request for the GetSpaceSummaryMetrics function.
# oneof collection_filter: exactly one of collection_name, collection_name_contains
GetSpaceSummaryMetricsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetSpaceSummaryMetricsRequestSpaceId = Annotated[
    str,
    "string.uuid = true",
]
GetSpaceSummaryMetricsRequestCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
GetSpaceSummaryMetricsRequestCollectionNameContains = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
GetSpaceSummaryMetricsRequestPageSize = Annotated[
    int,
    Gt(0),
    Le(250),
]
GetSpaceSummaryMetricsRequestPageToken = Annotated[
    str,
    MinLen(1),
]

# GetSpaceSummaryMetricsResponse is the response from the GetSpaceSummaryMetrics function.
GetSpaceSummaryMetricsResponseQuota = Annotated[
    SpaceQuotaSnapshot,
    "required",
]
GetSpaceSummaryMetricsResponseTotalSize = Annotated[
    int,
    Ge(0),
]
GetSpaceSummaryMetricsResponseNextPageToken = Annotated[
    str,
    MinLen(1),
]

# GetSpaceUsageMetricsRequest is the request for the GetSpaceUsageMetrics function.
# oneof collection_filter: exactly one of collection_name, collection_name_contains
# cel[get_space_usage_metrics.until_gt_since]: !has(this.since) || !has(this.until) || this.until > this.since
#   message: until must be after since
#   refs: since, this, until
#   calls: !_, _>_, _||_
GetSpaceUsageMetricsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetSpaceUsageMetricsRequestSpaceId = Annotated[
    str,
    "string.uuid = true",
]
GetSpaceUsageMetricsRequestAggregator = Annotated[
    AggregatorStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
GetSpaceUsageMetricsRequestCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
GetSpaceUsageMetricsRequestCollectionNameContains = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
GetSpaceUsageMetricsRequestPageSize = Annotated[
    int,
    Gt(0),
    Le(250),
]
GetSpaceUsageMetricsRequestPageToken = Annotated[
    str,
    MinLen(1),
]

# GetSpaceUsageMetricsResponse is the response from the GetSpaceUsageMetrics function.
GetSpaceUsageMetricsResponseQuota = Annotated[
    SpaceQuotaSnapshot,
    "required",
]
GetSpaceUsageMetricsResponseTotalSize = Annotated[
    int,
    Ge(0),
]
GetSpaceUsageMetricsResponseNextPageToken = Annotated[
    str,
    MinLen(1),
]

# GetSpaceInferenceMetricsRequest is the request for the GetSpaceInferenceMetrics function.
# oneof collection_filter: exactly one of collection_name, collection_name_contains
# cel[get_space_inference_metrics.until_gt_since]: !has(this.since) || !has(this.until) || this.until > this.since
#   message: until must be after since
#   refs: since, this, until
#   calls: !_, _>_, _||_
GetSpaceInferenceMetricsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetSpaceInferenceMetricsRequestSpaceId = Annotated[
    str,
    "string.uuid = true",
]
GetSpaceInferenceMetricsRequestInterval = Annotated[
    InferenceMetricsIntervalStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
GetSpaceInferenceMetricsRequestInferenceModelId = Annotated[
    str,
    MinLen(1),
]
GetSpaceInferenceMetricsRequestCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
GetSpaceInferenceMetricsRequestCollectionNameContains = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
GetSpaceInferenceMetricsRequestPageSize = Annotated[
    int,
    Gt(0),
    Le(250),
]
GetSpaceInferenceMetricsRequestPageToken = Annotated[
    str,
    MinLen(1),
]

# GetSpaceInferenceMetricsResponse is the response from the GetSpaceInferenceMetrics function.
GetSpaceInferenceMetricsResponseTotalSize = Annotated[
    int,
    Ge(0),
]
GetSpaceInferenceMetricsResponseNextPageToken = Annotated[
    str,
    MinLen(1),
]

# SpaceCollectionInferenceMetrics groups inference metrics for a single collection.
SpaceCollectionInferenceMetricsCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]

# SpaceInferenceModelMetrics groups histogram data for a single inference model.
SpaceInferenceModelMetricsInferenceModelId = Annotated[
    str,
    MinLen(1),
]

# ListSpaceAlertsRequest is the request for the ListSpaceAlerts function.
# oneof collection_filter: exactly one of collection_name, collection_name_contains, space_global_only
ListSpaceAlertsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListSpaceAlertsRequestSpaceId = Annotated[
    str,
    "string.uuid = true",
]
ListSpaceAlertsRequestCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
ListSpaceAlertsRequestCollectionNameContains = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
ListSpaceAlertsRequestSpaceGlobalOnly = Annotated[
    bool,
    "bool.const = true",
]
ListSpaceAlertsRequestPageSize = Annotated[
    int,
    Gt(0),
    Le(250),
]
ListSpaceAlertsRequestPageToken = Annotated[
    str,
    MinLen(1),
]

# ListSpaceAlertsResponse is the response from the ListSpaceAlerts function.
ListSpaceAlertsResponseTotalSize = Annotated[
    int,
    Ge(0),
]
ListSpaceAlertsResponseNextPageToken = Annotated[
    str,
    MinLen(1),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS SpaceAlert is a single alert instance for a space. Alerts may be space-global (no collection_name) or scoped to a collection.
SpaceAlertCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]

# SpaceQuotaSnapshot holds space-scoped effective quota ceilings and current collection count. A value of 0 on a ceiling field means unlimited for that dimension (same semantics as serverless-quota-sidecar GetSpaceLimits).
SpaceQuotaSnapshotMaxCollections = Annotated[
    int,
    Ge(0),
]
SpaceQuotaSnapshotCurrentCollections = Annotated[
    int,
    Ge(0),
]
SpaceQuotaSnapshotEffectiveMaxSizePerCollectionBytes = Annotated[
    int,
    Ge(0),
]
SpaceQuotaSnapshotSearcherEffectiveMaxWorkers = Annotated[
    int,
    Ge(0),
]

# SpaceCollectionMetrics contains a metric overview for a single collection.
SpaceCollectionMetricsCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]
SpaceCollectionMetricsSearchRequests = Annotated[
    SpaceMetricOverview,
    "required",
]
SpaceCollectionMetricsWriteRequests = Annotated[
    SpaceMetricOverview,
    "required",
]
SpaceCollectionMetricsSearchLatency = Annotated[
    SpaceMetricOverview,
    "required",
]
SpaceCollectionMetricsPointCount = Annotated[
    int,
    Ge(0),
]
SpaceCollectionMetricsUsedStorageBytes = Annotated[
    int,
    Ge(0),
]
SpaceCollectionMetricsCurrentSearcherWorkers = Annotated[
    int,
    Ge(0),
]

# SpaceMetricOverview contains average values of a metric over predefined intervals.

# IntervalAverage represents the average value of a metric over a specific time interval.
IntervalAverageInterval = Annotated[
    _google_protobuf_duration_pb2.Duration,
    "required",
]
IntervalAverageValue = Annotated[
    float,
    Ge(0),
]

# SpaceCollectionUsageMetrics defines per-collection usage metrics as timeseries.
SpaceCollectionUsageMetricsCollectionName = Annotated[
    str,
    MaxLen(255),
    MinLen(1),
    "string.pattern = ^[a-zA-Z0-9-_.]+$",
]

# A single metric data point.
MetricTimestamp = Annotated[
    _google_protobuf_timestamp_pb2.Timestamp,
    "required",
]
MetricValue = Annotated[
    float,
    Ge(0),
]

