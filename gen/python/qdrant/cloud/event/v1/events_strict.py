# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/event/v1/events.proto
"""buf.validate rules for qdrant/cloud/event/v1/events.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.event.v1.events_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from typing import Annotated

from annotated_types import Ge, MaxLen, MinLen

from qdrant.cloud.common.v1 import common_pb2 as _qdrant_cloud_common_v1_common_pb2
from qdrant.cloud.event.v1.events_pb2 import EventSource, EventType, ResultStatus

# EventType defines the type of the event that occurred to the resource.
# Without EVENT_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
EventTypeStrict = Annotated[
    EventType,
    Ge(1),
]

# ResultStatus identifies whether an audited RPC completed successfully.
# Without RESULT_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
ResultStatusStrict = Annotated[
    ResultStatus,
    Ge(1),
]

# EventSource identifies which API surface produced the event.
# Without EVENT_SOURCE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
EventSourceStrict = Annotated[
    EventSource,
    Ge(1),
]

# EventOptions is a custom method option to indicate that an event should be generated when this RPC is successfully called. The fields should start with 'req.' or 'resp.' to indicate whether the info needs to be extracted from the request or the response. Or 'req-md.' or 'resp-md.' to extract the info from the metadata.
EventOptionsEventType = Annotated[
    EventTypeStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
EventOptionsResourceType = Annotated[
    str,
    MinLen(1),
]
EventOptionsResourceIdField = Annotated[
    str,
    MinLen(1),
]
EventOptionsResourceUrlTemplate = Annotated[
    str,
    MinLen(1),
]
EventOptionsActionType = Annotated[
    str,
    MinLen(1),
]

# Result describes the outcome of the RPC that produced this event.
ResultStatus_ = Annotated[
    ResultStatusStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
ResultStatusCode = Annotated[
    int,
    Ge(0),
]
ResultErrorReason = Annotated[
    str,
    MaxLen(1024),
    MinLen(1),
]

# Event represents a significant occurrence in the system, like a resource being created, updated, or deleted.
EventId = Annotated[
    str,
    "string.uuid = true",
]
EventIpAddress = Annotated[
    str,
    "string.ipv4 = true",
]
EventCaller = Annotated[
    _qdrant_cloud_common_v1_common_pb2.Caller,
    "required",
]
EventAccountId = Annotated[
    str,
    "string.uuid = true",
]
EventSourceRpc = Annotated[
    str,
    MinLen(1),
]
EventSource_ = Annotated[
    EventSourceStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
EventTraceId = Annotated[
    str,
    MinLen(1),
]
EventEventType = Annotated[
    EventTypeStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
EventResourceType = Annotated[
    str,
    MinLen(1),
]
EventResourceId = Annotated[
    str,
    MinLen(1),
]
EventResourceUrl = Annotated[
    str,
    MinLen(1),
]
EventActionType = Annotated[
    str,
    MinLen(1),
]

