# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/booking/v1/booking.proto
"""buf.validate rules for qdrant/cloud/booking/v1/booking.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.booking.v1.booking_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from collections.abc import Sequence
from typing import Annotated

from annotated_types import Ge, Gt, Le, MaxLen, MinLen

from qdrant.cloud.booking.v1.booking_pb2 import AvailableStoragePerformanceTierConfigurations, GlobalQuoteConfiguration, ModelModality, Package, PackageStatus, PackageTier, VectorType

# PackageStatus defines the valid states a package can be in.
# Without PACKAGE_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
PackageStatusStrict = Annotated[
    PackageStatus,
    Ge(1),
]

# PackageTier defines the valid tiers a package can be in.
# Without PACKAGE_TIER_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
PackageTierStrict = Annotated[
    PackageTier,
    Ge(1),
]

# VectorType defines the type of vector representation.
# Without VECTOR_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
VectorTypeStrict = Annotated[
    VectorType,
    Ge(1),
]

# ModelModality defines the type of data the model processes.
# Without MODEL_MODALITY_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
ModelModalityStrict = Annotated[
    ModelModality,
    Ge(1),
]

# ListPackagesRequest is the request for the ListPackages function
# cel[list_packages.cloud_provider_region_id_present]: this.cloud_provider_id == 'hybrid' || has(this.cloud_provider_region_id)
#   message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
#   refs: cloud_provider_id, cloud_provider_region_id, this
#   calls: _==_, _||_
ListPackagesRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListPackagesRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
ListPackagesRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]
ListPackagesRequestTier = Annotated[
    PackageTierStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]

# ListPackagesResponse is the response from the ListPackages function

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_REQUEST_FIELDS ListGlobalPackagesRequest is the request for the ListGlobalPackages function
# cel[list_global_packages.cloud_provider_region_id_present]: this.cloud_provider_id == 'hybrid' || has(this.cloud_provider_region_id)
#   message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
#   refs: cloud_provider_id, cloud_provider_region_id, this
#   calls: _==_, _||_
ListGlobalPackagesRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
ListGlobalPackagesRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]
ListGlobalPackagesRequestTier = Annotated[
    PackageTierStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]

# ListPackagesResponse is the response from the ListPackages function

# GetPackageRequest is the request for the GetPackage function
# cel[list_packages.cloud_provider_region_id_present]: this.cloud_provider_id == 'hybrid' || has(this.cloud_provider_region_id)
#   message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
#   refs: cloud_provider_id, cloud_provider_region_id, this
#   calls: _==_, _||_
GetPackageRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetPackageRequestId = Annotated[
    str,
    "string.uuid = true",
]
GetPackageRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
GetPackageRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]

# GetPackageResponse is the response from the GetPackage function
GetPackageResponsePackage = Annotated[
    Package,
    "required",
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS Package represents a single package. A package is a configuration (CPU/Memory/Disk size) for a cluster with a price.
PackageId = Annotated[
    str,
    "string.uuid = true",
]
PackageName = Annotated[
    str,
    MaxLen(64),
    MinLen(3),
    "string.pattern = ^[a-zA-Z0-9-_]+$",
]
PackageType = Annotated[
    str,
    "string.in = [free, paid]",
]
PackageCurrency = Annotated[
    str,
    "string.pattern = ^[A-Z]{3}$",
]
PackageUnitIntPricePerHour = Annotated[
    int,
    Ge(0),
]
PackageStatus_ = Annotated[
    PackageStatusStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
PackageTier_ = Annotated[
    PackageTierStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
PackageAvailableStorageTierConfigurations = Annotated[
    Sequence[AvailableStoragePerformanceTierConfigurations],
    MinLen(1),
]

# AvailableAdditionalResources represents additional resources that can be added to the cluster.

# AvailableStoragePerformanceTierConfigurations represents available storage tier configurations for given package region and provider.

# ResourceConfiguration defines the resource configuration for a package.
ResourceConfigurationRam = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationCpu = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationDisk = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationGpu = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationGpuRam = Annotated[
    str,
    MinLen(1),
]

# ResourceConfigurationFilter defines the resource configuration filter for listing packages.
ResourceConfigurationFilterRam = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationFilterCpu = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationFilterDisk = Annotated[
    str,
    MinLen(1),
]
ResourceConfigurationFilterGpu = Annotated[
    str,
    MinLen(1),
]

# GetQuoteRequest is the request for the GetQuote function
# cel[get_quote.cloud_provider_region_id_present]: this.cloud_provider_id == 'hybrid' || has(this.cloud_provider_region_id)
#   message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
#   refs: cloud_provider_id, cloud_provider_region_id, this
#   calls: _==_, _||_
GetQuoteRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetQuoteRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
GetQuoteRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]
GetQuoteRequestNumberOfNodes = Annotated[
    int,
    Ge(1),
]
GetQuoteRequestPackageId = Annotated[
    str,
    "string.uuid = true",
]
GetQuoteRequestAdditionalDiskGib = Annotated[
    int,
    Ge(0),
]

# PriceBreakdown represents the price breakdown for one component of a quote. Amounts are in the same currency as the enclosing GetQuoteResponse.
PriceBreakdownOriginalPricePerHour = Annotated[
    int,
    Ge(0),
]
PriceBreakdownDiscountedPricePerHour = Annotated[
    int,
    Ge(0),
]
PriceBreakdownDiscountPercentage = Annotated[
    float,
    Ge(0),
    Le(100),
]

# GetQuoteResponse is the response from the GetQuote function
GetQuoteResponseCurrency = Annotated[
    str,
    "string.pattern = ^[A-Z]{3}$",
]
GetQuoteResponseOriginalPricePerHour = Annotated[
    int,
    Ge(0),
]
GetQuoteResponseDiscountedPricePerHour = Annotated[
    int,
    Ge(0),
]
GetQuoteResponseDiscountPercentage = Annotated[
    float,
    Ge(0),
    Le(100),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_REQUEST_FIELDS GetGlobalQuotesRequest is the request for the GetGlobalQuotes function
GetGlobalQuotesRequestConfigurations = Annotated[
    Sequence[GlobalQuoteConfiguration],
    MaxLen(50),
    MinLen(1),
]

# GlobalQuoteConfiguration describes a single cluster configuration to price as part of a GetGlobalQuotes request.
# cel[global_quote_configuration.cloud_provider_region_id_present]: this.cloud_provider_id == 'hybrid' || has(this.cloud_provider_region_id)
#   message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
#   refs: cloud_provider_id, cloud_provider_region_id, this
#   calls: _==_, _||_
GlobalQuoteConfigurationCloudProviderId = Annotated[
    str,
    MinLen(3),
]
GlobalQuoteConfigurationCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]
GlobalQuoteConfigurationNumberOfNodes = Annotated[
    int,
    Ge(1),
]
GlobalQuoteConfigurationPackageId = Annotated[
    str,
    "string.uuid = true",
]
GlobalQuoteConfigurationAdditionalDiskGib = Annotated[
    int,
    Ge(0),
]

# GlobalPriceBreakdown represents the list price for one component of a global quote. Amounts are in the same currency as the enclosing GlobalQuote.
GlobalPriceBreakdownPricePerHour = Annotated[
    int,
    Ge(0),
]

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS GlobalQuote is the list price for a single cluster configuration. It carries no discount fields: GetGlobalQuotes is unauthenticated and always quotes standard-tier list price, so a discount could only ever be reported as zero.
GlobalQuoteCurrency = Annotated[
    str,
    "string.pattern = ^[A-Z]{3}$",
]
GlobalQuotePricePerHour = Annotated[
    int,
    Ge(0),
]

# GetGlobalQuotesResponse is the response from the GetGlobalQuotes function

# GetBackupQuoteRequest is the request for the GetBackupQuote function
# cel[get_backup_quote.cloud_provider_region_id_present]: this.cloud_provider_id == 'hybrid' || has(this.cloud_provider_region_id)
#   message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
#   refs: cloud_provider_id, cloud_provider_region_id, this
#   calls: _==_, _||_
GetBackupQuoteRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
GetBackupQuoteRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
GetBackupQuoteRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]
GetBackupQuoteRequestBackupSizeGib = Annotated[
    int,
    Gt(0),
]

# GetBackupQuoteResponse is the response from the GetBackupQuote function
GetBackupQuoteResponseCurrency = Annotated[
    str,
    "string.pattern = ^[A-Z]{3}$",
]
GetBackupQuoteResponseOriginalPricePerHour = Annotated[
    int,
    Ge(0),
]
GetBackupQuoteResponseDiscountedPricePerHour = Annotated[
    int,
    Ge(0),
]
GetBackupQuoteResponseDiscountPercentage = Annotated[
    float,
    Ge(0),
    Le(100),
]

# ListInferenceModelsRequest is the request for the ListInferenceModels function
ListInferenceModelsRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListInferenceModelsRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
ListInferenceModelsRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]

# ListInferenceModelsResponse is the response from the ListInferenceModels function

# ListStorageTierTypesRequest is the request for the ListStorageTierTypes function
ListStorageTierTypesRequestAccountId = Annotated[
    str,
    "string.uuid = true",
]
ListStorageTierTypesRequestCloudProviderId = Annotated[
    str,
    MinLen(3),
]
ListStorageTierTypesRequestCloudProviderRegionId = Annotated[
    str,
    MinLen(1),
]

# ListStorageTierTypesResponse is the response from the ListStorageTierTypes function

# buf:lint:ignore QDRANT_CLOUD_REQUIRED_ENTITY_FIELDS InferenceModel represents a single inference model available for use.
InferenceModelId = Annotated[
    str,
    "string.uuid = true",
]
InferenceModelName = Annotated[
    str,
    MinLen(1),
]
InferenceModelTitle = Annotated[
    str,
    MinLen(1),
]
InferenceModelDescription = Annotated[
    str,
    MinLen(1),
]
InferenceModelVectorType = Annotated[
    VectorTypeStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
InferenceModelModality = Annotated[
    ModelModalityStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
InferenceModelVendor = Annotated[
    str,
    MinLen(1),
]
InferenceModelExternalDocsUrl = Annotated[
    str,
    "string.uri = true",
]

# StorageTiers holds information related to a particular storage tier

