# Code generated by protoc-gen-strict. DO NOT EDIT.
# source: qdrant/cloud/common/v1/common.proto
"""buf.validate rules for qdrant/cloud/common/v1/common.proto.

One typing.Annotated alias per constrained field, over the type
protoc-gen-python already declared. The message classes stay in
qdrant.cloud.common.v1.common_pb2; import them from there.

A rule with an annotated_types equivalent is carried as that constructor,
which pydantic, msgspec and beartype enforce. The rest are metadata strings,
left to protovalidate at runtime.
"""

from __future__ import annotations

from collections.abc import Mapping, Sequence
from typing import Annotated

from annotated_types import Ge, Gt, MaxLen, MinLen

from qdrant.cloud.common.v1.common_pb2 import ActorType, KeyValue, LabelSelectorRequirement, StorageTierType, TopologySpreadConstraintWhenUnsatisfiable

# ActorType specifies the type of actor that can call a method.
# Without ACTOR_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
ActorTypeStrict = Annotated[
    ActorType,
    Ge(1),
]

# StorageTierType defines type of storage performance tiers which maps to predefined IOPS and Throughput amount
# Without STORAGE_TIER_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
StorageTierTypeStrict = Annotated[
    StorageTierType,
    Ge(1),
]

# TopologySpreadConstraintWhenUnsatisfiable defines the valid whenUnsatisfiable for TopologySpreadConstraints. See: https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/
# Without TOPOLOGY_SPREAD_CONSTRAINT_WHEN_UNSATISFIABLE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
# reads as "unset" rather than a value. No buf.validate rule says so; the
# convention that names it <ENUM>_UNSPECIFIED does.
TopologySpreadConstraintWhenUnsatisfiableStrict = Annotated[
    TopologySpreadConstraintWhenUnsatisfiable,
    Ge(1),
]

# PatchSource describes how to construct a Get request from an Update request, enabling the API gateway to fetch the current resource for partial (FieldMask-based) updates.
PatchSourceGetMethod = Annotated[
    str,
    MinLen(1),
    "string.pattern = ^/[A-Za-z_][A-Za-z0-9_.]*/[A-Za-z_][A-Za-z0-9_]*$",
]
PatchSourceFieldMapping = Annotated[
    Mapping[str, str],
    "map.keys.string.pattern = ^[a-z_][a-z0-9_]*(\\.[a-z_][a-z0-9_]*)*$",
    "map.values.string.pattern = ^[a-z_][a-z0-9_]*(\\.[a-z_][a-z0-9_]*)*$",
]
PatchSourceResponseResourceField = Annotated[
    str,
    MinLen(1),
    "string.pattern = ^[a-z_][a-z0-9_]*(\\.[a-z_][a-z0-9_]*)*$",
]
PatchSourceRequestResourceField = Annotated[
    str,
    MinLen(1),
    "string.pattern = ^[a-z_][a-z0-9_]*(\\.[a-z_][a-z0-9_]*)*$",
]

# Caller identifies the authenticated actor that performed an action on a resource.
CallerActorId = Annotated[
    str,
    MinLen(1),
]
CallerActorType = Annotated[
    ActorTypeStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]
CallerUserAgent = Annotated[
    str,
    MaxLen(512),
]

# LogField represents a field to log (in case of an error).
LogFieldName = Annotated[
    str,
    MaxLen(64),
    MinLen(1),
    "string.pattern = ^[a-z][a-z_]*$",
]
LogFieldFieldExpression = Annotated[
    str,
    "string.pattern = ^(|[a-z_]+(\\.[a-z_]+)*)$",
]

# Semantic version number.

# SecretKeyRef is a reference to a Kubernetes secret name and the key inside the secret
SecretKeyRefName = Annotated[
    str,
    MaxLen(253),
    MinLen(1),
    "string.pattern = ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
]
SecretKeyRefKey = Annotated[
    str,
    MaxLen(1024),
    MinLen(1),
]

# KeyValue is a key-value tuple (used in e.g. node selectors / annotations) The message represents an object for Kubernetes.
KeyValueKey = Annotated[
    str,
    MaxLen(253),
    "string.pattern = ^([a-z0-9A-Z]([-a-z0-9A-Z]*[a-z0-9A-Z])?(\\.[a-z0-9A-Z]([-a-z0-9A-Z]*[a-z0-9A-Z])?)*\\/)?([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]$",
]
KeyValueValue = Annotated[
    str,
    MaxLen(1024),
]

# TopologySpreadConstraint specifies how to spread matching pods among the given topology.
TopologySpreadConstraintMaxSkew = Annotated[
    int,
    Gt(0),
]
TopologySpreadConstraintTopologyKey = Annotated[
    str,
    MinLen(1),
]
TopologySpreadConstraintWhenUnsatisfiable_ = Annotated[
    TopologySpreadConstraintWhenUnsatisfiableStrict,
    "enum.defined_only = true",
    "enum.not_in = [0]",
]

# A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values. This is a gRPC-native equivalent of the Kubernetes LabelSelectorRequirement.
# cel[label_selector_requirement.values_for_in_not-in]: !(this.operator == 'In' || this.operator == 'NotIn') || this.values.size() > 0
#   message: values must be non-empty when operator is In or NotIn
#   refs: operator, this, values
#   calls: !_, _==_, _>_, _||_, size
# cel[label_selector_requirement.values_for_exists_not-exists]: !(this.operator == 'Exists' || this.operator == 'DoesNotExist') || this.values.size() == 0
#   message: values must be empty when operator is Exists or DoesNotExist
#   refs: operator, this, values
#   calls: !_, _==_, _||_, size
LabelSelectorRequirementKey = Annotated[
    str,
    MinLen(1),
]
LabelSelectorRequirementOperator = Annotated[
    str,
    "string.in = [In, NotIn, Exists, DoesNotExist]",
]

# A label selector is a label query over a set of resources. The result of match_labels and match_expressions are AND-ed. An empty label selector matches all objects. This is a gRPC-native equivalent of the Kubernetes LabelSelector.
LabelSelectorMatchLabels = Annotated[
    Sequence[KeyValue],
    MaxLen(10),
]
LabelSelectorMatchExpressions = Annotated[
    Sequence[LabelSelectorRequirement],
    MaxLen(10),
]

# IPBlock describes a particular CIDR (e.g. "192.168.1.1/24") that is allowed to the pods. This is a gRPC-native equivalent of the Kubernetes IPBlock.
IPBlockCidr = Annotated[
    str,
    "string.ip_prefix = true",
]
IPBlockExcept = Annotated[
    Sequence[str],
    "repeated.items.string.ip_prefix = true",
]

# PeerSelector selects pods and/or namespaces.

# NetworkPolicyPeer describes a peer to allow traffic to/from. This is a gRPC-native equivalent of the Kubernetes NetworkPolicyPeer.
# oneof peer_type: exactly one of selector, ip_block

# NetworkPolicyPort describes a port to allow traffic on. This is a gRPC-native equivalent of the Kubernetes NetworkPolicyPort.
# oneof port_spec: exactly one of port_number, port_name
# cel[network_policy_port.end_port_with_port_name]: !has(this.port_name) || !has(this.end_port)
#   message: end_port cannot be set when port_name is used
#   refs: end_port, port_name, this
#   calls: !_, _||_
# cel[network_policy_port.end_port_ge_port_number]: !has(this.end_port) || !has(this.port_number) || this.end_port >= this.port_number
#   message: end_port must be greater than or equal to port_number
#   refs: end_port, port_number, this
#   calls: !_, _>=_, _||_
NetworkPolicyPortProtocol = Annotated[
    str,
    "string.in = [TCP, UDP, SCTP]",
]
NetworkPolicyPortPortNumber = Annotated[
    int,
    Gt(0),
]
NetworkPolicyPortPortName = Annotated[
    str,
    MinLen(1),
]
NetworkPolicyPortEndPort = Annotated[
    int,
    Gt(0),
]

# NetworkPolicyIngressRule describes a particular set of traffic that is allowed to the pods matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and from. This is a gRPC-native equivalent of the Kubernetes NetworkPolicyIngressRule.

# NetworkPolicyEgressRule describes a particular set of traffic that is allowed out of pods matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and to. This is a gRPC-native equivalent of the Kubernetes NetworkPolicyEgressRule.

