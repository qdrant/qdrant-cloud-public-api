// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/serverless/collections.proto

import type { Narrow } from "../../strict/types";
import type { BoolIndex, CollectionConfig, CollectionSummary, CreateCollectionRequest, CreateCollectionResponse, DatetimeIndex, DeleteCollectionRequest, DeleteCollectionResponse, DenseVectorConfig, DisabledStemmer, Distance, FloatIndex, GeoIndex, GetCollectionRequest, GetCollectionResponse, IntegerIndex, KeywordIndex, KeywordPrefixParams, ListCollectionsRequest, ListCollectionsResponse, PayloadIndexConfig, PrecisionTier, SnowballParams, SparseVectorConfig, StemmingAlgorithm, StopwordsSet, TextIndex, Tokenizer, UuidIndex } from "./collections_pb";
import { CollectionsService, CreateCollectionRequestSchema, CreateCollectionResponseSchema, DeleteCollectionRequestSchema, DeleteCollectionResponseSchema, DenseVectorConfigSchema, GetCollectionRequestSchema, GetCollectionResponseSchema, ListCollectionsRequestSchema, ListCollectionsResponseSchema, PayloadIndexConfigSchema, SparseVectorConfigSchema, TextIndexSchema } from "./collections_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/**
 * Distance metric used to compare dense vectors.
 *
 * Without DISTANCE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type DistanceStrict = Exclude<Distance, 0>;

/**
 * How much of the original vector precision may be traded for cost. The manager turns this into a concrete quantization / datatype choice; the tenant never picks scalar/product/binary quantization or its parameters directly.
 *
 * Without PRECISION_TIER_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type PrecisionTierStrict = Exclude<PrecisionTier, 0>;

/**
 * Full-text tokenizer, mirrors qdrant's `TokenizerType`.
 *
 * Without TOKENIZER_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type TokenizerStrict = Exclude<Tokenizer, 0>;

/** Configuration of a single dense (embedding) vector. */
export type DenseVectorConfigStrict = Narrow<DenseVectorConfig, {
  distance: DistanceStrict;
  precisionTier: PrecisionTierStrict;
}>;

/** Configuration of a single sparse vector. */
export type SparseVectorConfigStrict = Narrow<SparseVectorConfig, {
  precisionTier: PrecisionTierStrict;
}>;

/** Exact match on string values, e.g. `color: "red"`. */
export type KeywordIndexStrict = KeywordIndex;

/** Prefix matching options for the keyword index. Has no options yet: presence of this message enables prefix matching. */
export type KeywordPrefixParamsStrict = KeywordPrefixParams;

/** Exact match and/or range filters on integers, e.g. `age: 25`. Both are on by default; turning one off shrinks the index. */
export type IntegerIndexStrict = IntegerIndex;

/** Range filters on floating point (and integer) numbers, e.g. `price: 99.5`. */
export type FloatIndexStrict = FloatIndex;

/** Exact match on UUID strings; like keyword but stored compactly. */
export type UuidIndexStrict = UuidIndex;

/** Range filters on RFC 3339 datetimes, e.g. `created_at: "2023-02-08T10:49:00Z"`. */
export type DatetimeIndexStrict = DatetimeIndex;

/** Tokens ignored by a full-text index. Language names match qdrant (e.g. "english"); predefined lists and custom tokens are merged. */
export type StopwordsSetStrict = StopwordsSet;

/** Snowball stemming for a full-text index. */
export type SnowballParamsStrict = SnowballParams;

/** Explicitly disable stemming (overrides any language default). */
export type DisabledStemmerStrict = DisabledStemmer;

/** Stemming algorithm for a full-text index. Unset: no stemming. */
export type StemmingAlgorithmStrict = StemmingAlgorithm;

/** Full-text filtering on string values. */
export type TextIndexStrict = Narrow<TextIndex, {
  tokenizer: TokenizerStrict;
}>;

/** Geo radius / bounding box / polygon filters on `{lon, lat}` values. */
export type GeoIndexStrict = GeoIndex;

/** Exact match on booleans, e.g. `is_active: true`. */
export type BoolIndexStrict = BoolIndex;

/** One payload index. Only the *kind* of filter the field supports is chosen here; storage placement of the index is the manager's decision. */
export type PayloadIndexConfigStrict = PayloadIndexConfig;

/** The tenant's collection config. Persisted verbatim. At least one dense or sparse vector is required. */
export type CollectionConfigStrict = CollectionConfig;

/** Every request names the collection by its tenant-facing name only. The tenant (`x-account-id`, `x-space-id`) travels in gRPC metadata, injected by auth. The storage id is the manager's: minted on create, resolved internally on get/delete. */
export type CreateCollectionRequestStrict = CreateCollectionRequest;

/** Result of a create. */
export type CreateCollectionResponseStrict = CreateCollectionResponse;

/** Names the collection to delete. */
export type DeleteCollectionRequestStrict = DeleteCollectionRequest;

/** Result of a delete. */
export type DeleteCollectionResponseStrict = DeleteCollectionResponse;

/** Names the collection to fetch. */
export type GetCollectionRequestStrict = GetCollectionRequest;

/** The collection's configuration and stats, if it exists. */
export type GetCollectionResponseStrict = GetCollectionResponse;

/**
 * Lists the caller's collections. The tenant travels in metadata.
 *
 * Left to runtime validation, having no type equivalent:
 *   limit
 *     uint32.gt = 0
 *     uint32.lte = 100
 */
export type ListCollectionsRequestStrict = ListCollectionsRequest;

/** One collection in a listing. */
export type CollectionSummaryStrict = CollectionSummary;

/** The caller's collections. */
export type ListCollectionsResponseStrict = ListCollectionsResponse;

/**
 * Describes qdrant.serverless.DenseVectorConfig, reporting DenseVectorConfigStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const DenseVectorConfigStrictSchema =
  DenseVectorConfigSchema as GenMessage<DenseVectorConfig, { validType: DenseVectorConfigStrict }>;

/**
 * Describes qdrant.serverless.SparseVectorConfig, reporting SparseVectorConfigStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SparseVectorConfigStrictSchema =
  SparseVectorConfigSchema as GenMessage<SparseVectorConfig, { validType: SparseVectorConfigStrict }>;

/**
 * Describes qdrant.serverless.TextIndex, reporting TextIndexStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const TextIndexStrictSchema =
  TextIndexSchema as GenMessage<TextIndex, { validType: TextIndexStrict }>;

/**
 * Describes qdrant.serverless.PayloadIndexConfig, reporting PayloadIndexConfigStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const PayloadIndexConfigStrictSchema =
  PayloadIndexConfigSchema as GenMessage<PayloadIndexConfig, { validType: PayloadIndexConfigStrict }>;

type CollectionsServiceStrictDescriptor = GenService<{
  /** Creates a collection with the given tenant-facing configuration. */
  createCollection: {
    methodKind: "unary";
    input: typeof CreateCollectionRequestSchema;
    output: typeof CreateCollectionResponseSchema;
  },
  /** Deletes a collection and all of its data. */
  deleteCollection: {
    methodKind: "unary";
    input: typeof DeleteCollectionRequestSchema;
    output: typeof DeleteCollectionResponseSchema;
  },
  /** Returns a single collection's configuration and stats. */
  getCollection: {
    methodKind: "unary";
    input: typeof GetCollectionRequestSchema;
    output: typeof GetCollectionResponseSchema;
  },
  /** Lists the caller's collections. */
  listCollections: {
    methodKind: "unary";
    input: typeof ListCollectionsRequestSchema;
    output: typeof ListCollectionsResponseSchema;
  },
}>;

/**
 * CollectionsService manages the collections of a qdrant serverless space. Unlike the qdrant server API, it exposes only a simplified configuration: the manager turns it into concrete qdrant settings, which are never exposed to the tenant.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const CollectionsServiceStrict =
  CollectionsService as CollectionsServiceStrictDescriptor;

