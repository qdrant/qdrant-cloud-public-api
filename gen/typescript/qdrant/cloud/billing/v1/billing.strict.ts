// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/billing/v1/billing.proto

import type { Narrow, Require, Uuid } from "../../../../strict/types";
import type { CreditContract, Discount, DiscountFixed, DiscountPercentage, GetBillingAccountParentRequest, GetBillingAccountParentResponse, Invoice, InvoiceStatus, ListBillingAccountChildrenRequest, ListBillingAccountChildrenResponse, ListCreditContractsRequest, ListCreditContractsResponse, ListDiscountsRequest, ListDiscountsResponse, ListInvoicesRequest, ListInvoicesResponse, MonthlyCreditConsumption } from "./billing_pb";
import { BillingService, CreditContractSchema, DiscountSchema, GetBillingAccountParentRequestSchema, GetBillingAccountParentResponseSchema, InvoiceSchema, ListBillingAccountChildrenRequestSchema, ListBillingAccountChildrenResponseSchema, ListCreditContractsRequestSchema, ListCreditContractsResponseSchema, ListDiscountsRequestSchema, ListDiscountsResponseSchema, ListInvoicesRequestSchema, ListInvoicesResponseSchema } from "./billing_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/**
 * The possible status of an invoice.
 *
 * Without INVOICE_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type InvoiceStatusStrict = Exclude<InvoiceStatus, 0>;

/** ListInvoicesRequest is the request for the ListInvoices function */
export type ListInvoicesRequestStrict = Narrow<ListInvoicesRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListInvoicesResponse is the response from the ListInvoices function */
export type ListInvoicesResponseStrict = Narrow<ListInvoicesResponse, {
  items: InvoiceStrict[];
}>;

/** ListDiscountsRequest is the request for the ListDiscounts function */
export type ListDiscountsRequestStrict = Narrow<ListDiscountsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListDiscountsResponse is the response from the ListDiscounts function */
export type ListDiscountsResponseStrict = Narrow<ListDiscountsResponse, {
  items: DiscountStrict[];
}>;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   id
 *     string.min_len = 1
 *   number
 *     string.min_len = 1
 *   total_amount
 *     int64.gte = 0
 *   pdf_url
 *     string.uri = true
 *   subtotal_amount
 *     int64.gte = 0
 *   amount_due
 *     int64.gte = 0
 */
export type InvoiceStrict = Require<Narrow<Invoice, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  createdAt: NonNullable<Invoice["createdAt"]>;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  status: InvoiceStatusStrict;
}>, "createdAt">;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[discount.valid_period]
 *     this.valid_until > this.valid_from
 *     message: valid_until must be after valid_from
 *   name
 *     string.min_len = 1
 */
export type DiscountStrict = Require<Narrow<Discount, {
  /** required oneof type: exactly one of percentage, fixed */
  type: Exclude<Discount["type"], { case: undefined }>;
  /**
   * required
   *
   * Carried into the type: required.
   */
  validFrom: NonNullable<Discount["validFrom"]>;
  /**
   * required
   * timestamp.gt_now = false
   *
   * Carried into the type: required.
   */
  validUntil: NonNullable<Discount["validUntil"]>;
}>, "validFrom" | "validUntil">;

/**
 * DiscountPercentage represents a percentage-based discount.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gte = 0
 *     double.lte = 100
 */
export type DiscountPercentageStrict = DiscountPercentage;

/**
 * DiscountFixed represents a fixed amount discount.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gt = 0
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 */
export type DiscountFixedStrict = DiscountFixed;

/** ListCreditContractsRequest is the request for the ListCreditContracts function */
export type ListCreditContractsRequestStrict = Narrow<ListCreditContractsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListCreditContractsResponse is the response from the ListCreditContracts function */
export type ListCreditContractsResponseStrict = Narrow<ListCreditContractsResponse, {
  items: CreditContractStrict[];
}>;

/**
 * MonthlyCreditConsumption represents a month's consumed credits.
 *
 * Left to runtime validation, having no type equivalent:
 *   year
 *     int32.gte = 2000
 *     int32.lte = 3000
 *   month
 *     int32.gte = 1
 *     int32.lte = 12
 *   amount
 *     double.gte = 0
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 */
export type MonthlyCreditConsumptionStrict = MonthlyCreditConsumption;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   total_amount
 *     double.gt = 0
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 *   notes
 *     string.min_len = 1
 *   used_amount
 *     double.gte = 0
 *   remaining_amount
 *     double.gte = 0
 *   voided_amount
 *     double.gte = 0
 */
export type CreditContractStrict = Require<Narrow<CreditContract, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  id: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * required
   *
   * Carried into the type: required.
   */
  activeFrom: NonNullable<CreditContract["activeFrom"]>;
  /**
   * required
   *
   * Carried into the type: required.
   */
  activeTo: NonNullable<CreditContract["activeTo"]>;
}>, "activeFrom" | "activeTo">;

/** GetBillingAccountParentRequest is the request for the GetBillingAccountParent RPC. */
export type GetBillingAccountParentRequestStrict = Narrow<GetBillingAccountParentRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

export type GetBillingAccountParentResponseStrict = Narrow<GetBillingAccountParentResponse, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  parentAccountId: Uuid;
}>;

/** ListBillingAccountChildrenRequest is the request for the ListBillingAccountChildren RPC. */
export type ListBillingAccountChildrenRequestStrict = Narrow<ListBillingAccountChildrenRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   child_account_ids
 *     repeated.items.string.uuid = true
 */
export type ListBillingAccountChildrenResponseStrict = ListBillingAccountChildrenResponse;

/**
 * Describes qdrant.cloud.billing.v1.ListInvoicesRequest, reporting ListInvoicesRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListInvoicesRequestStrictSchema =
  ListInvoicesRequestSchema as GenMessage<ListInvoicesRequest, { validType: ListInvoicesRequestStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.ListInvoicesResponse, reporting ListInvoicesResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListInvoicesResponseStrictSchema =
  ListInvoicesResponseSchema as GenMessage<ListInvoicesResponse, { validType: ListInvoicesResponseStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.ListDiscountsRequest, reporting ListDiscountsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListDiscountsRequestStrictSchema =
  ListDiscountsRequestSchema as GenMessage<ListDiscountsRequest, { validType: ListDiscountsRequestStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.ListDiscountsResponse, reporting ListDiscountsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListDiscountsResponseStrictSchema =
  ListDiscountsResponseSchema as GenMessage<ListDiscountsResponse, { validType: ListDiscountsResponseStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.Invoice, reporting InvoiceStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const InvoiceStrictSchema =
  InvoiceSchema as GenMessage<Invoice, { validType: InvoiceStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.Discount, reporting DiscountStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const DiscountStrictSchema =
  DiscountSchema as GenMessage<Discount, { validType: DiscountStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.ListCreditContractsRequest, reporting ListCreditContractsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListCreditContractsRequestStrictSchema =
  ListCreditContractsRequestSchema as GenMessage<ListCreditContractsRequest, { validType: ListCreditContractsRequestStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.ListCreditContractsResponse, reporting ListCreditContractsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListCreditContractsResponseStrictSchema =
  ListCreditContractsResponseSchema as GenMessage<ListCreditContractsResponse, { validType: ListCreditContractsResponseStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.CreditContract, reporting CreditContractStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const CreditContractStrictSchema =
  CreditContractSchema as GenMessage<CreditContract, { validType: CreditContractStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.GetBillingAccountParentRequest, reporting GetBillingAccountParentRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetBillingAccountParentRequestStrictSchema =
  GetBillingAccountParentRequestSchema as GenMessage<GetBillingAccountParentRequest, { validType: GetBillingAccountParentRequestStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.GetBillingAccountParentResponse, reporting GetBillingAccountParentResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetBillingAccountParentResponseStrictSchema =
  GetBillingAccountParentResponseSchema as GenMessage<GetBillingAccountParentResponse, { validType: GetBillingAccountParentResponseStrict }>;

/**
 * Describes qdrant.cloud.billing.v1.ListBillingAccountChildrenRequest, reporting ListBillingAccountChildrenRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListBillingAccountChildrenRequestStrictSchema =
  ListBillingAccountChildrenRequestSchema as GenMessage<ListBillingAccountChildrenRequest, { validType: ListBillingAccountChildrenRequestStrict }>;

type BillingServiceStrictDescriptor = GenService<{
  /** Lists all invoices for the account identified by the given ID. Required permissions: - read:payment_information */
  listInvoices: {
    methodKind: "unary";
    input: typeof ListInvoicesRequestStrictSchema;
    output: typeof ListInvoicesResponseStrictSchema;
  },
  /** Lists all discounts for the account identified by the given ID. Required permissions: - read:payment_information */
  listDiscounts: {
    methodKind: "unary";
    input: typeof ListDiscountsRequestStrictSchema;
    output: typeof ListDiscountsResponseStrictSchema;
  },
  /** Lists all credit contracts for the account identified by the given ID. Required permissions: - read:payment_information */
  listCreditContracts: {
    methodKind: "unary";
    input: typeof ListCreditContractsRequestStrictSchema;
    output: typeof ListCreditContractsResponseStrictSchema;
  },
  /** Returns the parent billing account for the account identified by the given ID. Returns NOT_FOUND if the account has no parent. Required permissions: - read:payment_information */
  getBillingAccountParent: {
    methodKind: "unary";
    input: typeof GetBillingAccountParentRequestStrictSchema;
    output: typeof GetBillingAccountParentResponseStrictSchema;
  },
  /** Lists all child accounts for the billing parent account identified by the given ID. Returns an empty list if the account has no children. Required permissions: - read:payment_information */
  listBillingAccountChildren: {
    methodKind: "unary";
    input: typeof ListBillingAccountChildrenRequestStrictSchema;
    output: typeof ListBillingAccountChildrenResponseSchema;
  },
}>;

/**
 * BillingService is the API used to retrieve billing information, such as invoices.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const BillingServiceStrict =
  BillingService as BillingServiceStrictDescriptor;

