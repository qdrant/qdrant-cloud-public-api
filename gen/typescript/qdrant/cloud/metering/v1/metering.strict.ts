// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/metering/v1/metering.proto

import type { Narrow, Require, Uuid } from "../../../../strict/types";
import type { BackupStorageConfig, ClusterExtraDiskConfig, ClusterStorageTierConfig, ClusterUsageConfig, CreditDrawdown, GetUsageBreakdownRequest, GetUsageBreakdownResponse, InferenceConfig, ListMeteringsRequest, ListMeteringsResponse, ListMonthlyMeteringsRequest, ListMonthlyMeteringsResponse, MeteringItem, MonthlyMeteringSummary, UsageBreakdownAccount, UsageBreakdownCluster, UsageBreakdownFunding, UsageBreakdownMonth } from "./metering_pb";
import { GetUsageBreakdownRequestSchema, GetUsageBreakdownResponseSchema, ListMeteringsRequestSchema, ListMeteringsResponseSchema, ListMonthlyMeteringsRequestSchema, ListMonthlyMeteringsResponseSchema, MeteringItemSchema, MeteringService, UsageBreakdownAccountSchema, UsageBreakdownClusterSchema, UsageBreakdownMonthSchema } from "./metering_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/** ListMonthlyMeteringsRequest is the request for the ListMonthlyMeterings function */
export type ListMonthlyMeteringsRequestStrict = Narrow<ListMonthlyMeteringsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListMonthlyMeterings is the response from the ListMonthlyMeterings function */
export type ListMonthlyMeteringsResponseStrict = ListMonthlyMeteringsResponse;

/**
 * ListMeteringsRequest is the request for the ListMeterings function
 *
 * Left to runtime validation, having no type equivalent:
 *   year
 *     int32.gte = 2000
 *     int32.lte = 3000
 *   month
 *     int32.gte = 1
 *     int32.lte = 12
 */
export type ListMeteringsRequestStrict = Narrow<ListMeteringsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListMeteringsResponse is the response from the ListMeterings function */
export type ListMeteringsResponseStrict = Narrow<ListMeteringsResponse, {
  items: MeteringItemStrict[];
}>;

/**
 * MonthlyMeteringSummary represents a single month's metering summary.
 *
 * Left to runtime validation, having no type equivalent:
 *   year
 *     int32.gte = 2000
 *     int32.lte = 3000
 *   month
 *     int32.gte = 1
 *     int32.lte = 12
 *   amount_millicents
 *     int64.gte = 0
 *   currency
 *     string.len = 3
 *     string.pattern = ^[A-Z]{3}$
 */
export type MonthlyMeteringSummaryStrict = MonthlyMeteringSummary;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[metering_item.end_time_gt_start_time]
 *     this.end_time > this.start_time
 *     message: end_time must be after start_time
 *   cluster_name
 *     string.min_len = 1
 *   billable_entity_reference_name
 *     string.min_len = 1
 *   billable_entity_type
 *     string.min_len = 1
 *   price_per_hour
 *     int32.gte = 0
 *   usage_hours
 *     double.gte = 0
 *   amount_millicents
 *     int64.gte = 0
 *   discount_amount_millicents
 *     int64.gte = 0
 *   discount_amount_percent
 *     double.gte = 0
 *     double.lte = 100
 *   currency
 *     string.len = 3
 *     string.pattern = ^[A-Z]{3}$
 */
export type MeteringItemStrict = Require<Narrow<MeteringItem, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
  /**
   * required
   *
   * Carried into the type: required.
   */
  startTime: NonNullable<MeteringItem["startTime"]>;
  /**
   * required
   *
   * Carried into the type: required.
   */
  endTime: NonNullable<MeteringItem["endTime"]>;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  billableEntityId: Uuid;
}>, "startTime" | "endTime">;

/**
 * GetUsageBreakdownRequest is the request for the GetUsageBreakdown function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_usage_breakdown_request.end_time_gt_start_time]
 *     this.end_time > this.start_time
 *     message: end_time must be after start_time
 */
export type GetUsageBreakdownRequestStrict = Require<Narrow<GetUsageBreakdownRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * required
   *
   * Carried into the type: required.
   */
  startTime: NonNullable<GetUsageBreakdownRequest["startTime"]>;
  /**
   * required
   *
   * Carried into the type: required.
   */
  endTime: NonNullable<GetUsageBreakdownRequest["endTime"]>;
}>, "startTime" | "endTime">;

/** GetUsageBreakdownResponse is the response from the GetUsageBreakdown function */
export type GetUsageBreakdownResponseStrict = Narrow<GetUsageBreakdownResponse, {
  items: UsageBreakdownMonthStrict[];
}>;

/**
 * UsageBreakdownMonth groups usage for a single calendar month.
 *
 * Left to runtime validation, having no type equivalent:
 *   year
 *     int32.gte = 2000
 *     int32.lte = 3000
 *   month
 *     int32.gte = 1
 *     int32.lte = 12
 */
export type UsageBreakdownMonthStrict = Narrow<UsageBreakdownMonth, {
  accounts: UsageBreakdownAccountStrict[];
}>;

export type UsageBreakdownFundingStrict = UsageBreakdownFunding;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   credit_contract_id
 *     string.min_len = 1
 *   amount_millicents
 *     int64.gte = 0
 */
export type CreditDrawdownStrict = CreditDrawdown;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   account_name
 *     string.min_len = 1
 */
export type UsageBreakdownAccountStrict = Narrow<UsageBreakdownAccount, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  clusters: UsageBreakdownClusterStrict[];
}>;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cpu_vcpu
 *     int32.gte = 0
 *   ram_gib
 *     int32.gte = 0
 *   disk_gib
 *     int32.gte = 0
 *   node_count
 *     int32.gte = 0
 */
export type ClusterUsageConfigStrict = ClusterUsageConfig;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   extra_disk_gib
 *     int32.gte = 0
 */
export type ClusterExtraDiskConfigStrict = ClusterExtraDiskConfig;

export type ClusterStorageTierConfigStrict = ClusterStorageTierConfig;

export type BackupStorageConfigStrict = BackupStorageConfig;

export type InferenceConfigStrict = InferenceConfig;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[usage_breakdown_cluster.end_time_gt_start_time]
 *     this.end_time > this.start_time
 *     message: end_time must be after start_time
 *   cluster_name
 *     string.min_len = 1
 *   amount_millicents
 *     int64.gte = 0
 *   currency
 *     string.len = 3
 *     string.pattern = ^[A-Z]{3}$
 *   billable_entity_type
 *     string.min_len = 1
 *   quantity
 *     double.gte = 0
 *   discount_amount_millicents
 *     int64.gte = 0
 *   discount_amount_percent
 *     double.gte = 0
 *     double.lte = 100
 */
export type UsageBreakdownClusterStrict = Require<Narrow<UsageBreakdownCluster, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
  /**
   * required
   *
   * Carried into the type: required.
   */
  startTime: NonNullable<UsageBreakdownCluster["startTime"]>;
  /**
   * required
   *
   * Carried into the type: required.
   */
  endTime: NonNullable<UsageBreakdownCluster["endTime"]>;
}>, "startTime" | "endTime">;

/**
 * Describes qdrant.cloud.metering.v1.ListMonthlyMeteringsRequest, reporting ListMonthlyMeteringsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListMonthlyMeteringsRequestStrictSchema =
  ListMonthlyMeteringsRequestSchema as GenMessage<ListMonthlyMeteringsRequest, { validType: ListMonthlyMeteringsRequestStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.ListMeteringsRequest, reporting ListMeteringsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListMeteringsRequestStrictSchema =
  ListMeteringsRequestSchema as GenMessage<ListMeteringsRequest, { validType: ListMeteringsRequestStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.ListMeteringsResponse, reporting ListMeteringsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListMeteringsResponseStrictSchema =
  ListMeteringsResponseSchema as GenMessage<ListMeteringsResponse, { validType: ListMeteringsResponseStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.MeteringItem, reporting MeteringItemStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const MeteringItemStrictSchema =
  MeteringItemSchema as GenMessage<MeteringItem, { validType: MeteringItemStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.GetUsageBreakdownRequest, reporting GetUsageBreakdownRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetUsageBreakdownRequestStrictSchema =
  GetUsageBreakdownRequestSchema as GenMessage<GetUsageBreakdownRequest, { validType: GetUsageBreakdownRequestStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.GetUsageBreakdownResponse, reporting GetUsageBreakdownResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetUsageBreakdownResponseStrictSchema =
  GetUsageBreakdownResponseSchema as GenMessage<GetUsageBreakdownResponse, { validType: GetUsageBreakdownResponseStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.UsageBreakdownMonth, reporting UsageBreakdownMonthStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const UsageBreakdownMonthStrictSchema =
  UsageBreakdownMonthSchema as GenMessage<UsageBreakdownMonth, { validType: UsageBreakdownMonthStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.UsageBreakdownAccount, reporting UsageBreakdownAccountStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const UsageBreakdownAccountStrictSchema =
  UsageBreakdownAccountSchema as GenMessage<UsageBreakdownAccount, { validType: UsageBreakdownAccountStrict }>;

/**
 * Describes qdrant.cloud.metering.v1.UsageBreakdownCluster, reporting UsageBreakdownClusterStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const UsageBreakdownClusterStrictSchema =
  UsageBreakdownClusterSchema as GenMessage<UsageBreakdownCluster, { validType: UsageBreakdownClusterStrict }>;

type MeteringServiceStrictDescriptor = GenService<{
  /** Lists aggregated monthly meterings for the account identified by the given ID. Each summary represents the total metering amount for one month. Required permissions: - read:payment_information */
  listMonthlyMeterings: {
    methodKind: "unary";
    input: typeof ListMonthlyMeteringsRequestStrictSchema;
    output: typeof ListMonthlyMeteringsResponseSchema;
  },
  /** Lists detailed metering entries for a specific month for the account identified by the given ID. Required permissions: - read:payment_information */
  listMeterings: {
    methodKind: "unary";
    input: typeof ListMeteringsRequestStrictSchema;
    output: typeof ListMeteringsResponseStrictSchema;
  },
  /** Returns the usage breakdown for the account identified by the given ID, within the requested time range. For parent billing accounts, the response aggregates usage across all child accounts. Required permissions: - read:payment_information */
  getUsageBreakdown: {
    methodKind: "unary";
    input: typeof GetUsageBreakdownRequestStrictSchema;
    output: typeof GetUsageBreakdownResponseStrictSchema;
  },
}>;

/**
 * MeteringService is the API to retrieve usage data used for billing purposes.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const MeteringServiceStrict =
  MeteringService as MeteringServiceStrictDescriptor;

