// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/monitoring/v1/monitoring.proto

import type { Narrow, Require, Uuid } from "../../../../strict/types";
import type { Aggregator, ClusterAlert, ClusterAlertSeverity, ClusterAlertState, ClusterAlertType, ClusterInferenceModelMetrics, ClusterMetricOverview, ClusterNodeMetrics, ClusterNodeUsageMetrics, GetClusterEventsRequest, GetClusterEventsResponse, GetClusterInferenceMetricsRequest, GetClusterInferenceMetricsResponse, GetClusterLogsRequest, GetClusterLogsResponse, GetClusterSummaryMetricsRequest, GetClusterSummaryMetricsResponse, GetClusterUsageMetricsRequest, GetClusterUsageMetricsResponse, InferenceMetricsInterval, IntervalAverage, ListClusterAlertsRequest, ListClusterAlertsResponse, LogEntry, Metric, ResourceValue } from "./monitoring_pb";
import { ClusterAlertSchema, ClusterInferenceModelMetricsSchema, ClusterMetricOverviewSchema, ClusterNodeMetricsSchema, ClusterNodeUsageMetricsSchema, GetClusterEventsRequestSchema, GetClusterEventsResponseSchema, GetClusterInferenceMetricsRequestSchema, GetClusterInferenceMetricsResponseSchema, GetClusterLogsRequestSchema, GetClusterLogsResponseSchema, GetClusterSummaryMetricsRequestSchema, GetClusterSummaryMetricsResponseSchema, GetClusterUsageMetricsRequestSchema, GetClusterUsageMetricsResponseSchema, IntervalAverageSchema, ListClusterAlertsRequestSchema, ListClusterAlertsResponseSchema, LogEntrySchema, MetricSchema, MonitoringService } from "./monitoring_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/**
 * Aggregator defines how metrics should be aggregated over time.
 *
 * Without AGGREGATOR_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type AggregatorStrict = Exclude<Aggregator, 0>;

/**
 * InferenceMetricsInterval defines the histogram bucket size for inference metrics.
 *
 * Without INFERENCE_METRICS_INTERVAL_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type InferenceMetricsIntervalStrict = Exclude<InferenceMetricsInterval, 0>;

/**
 * ClusterAlertState mirrors the backend state of a cluster alert. It is used both as the optional request-side filter AND echoed per row on the ClusterAlert response.
 *
 * Without CLUSTER_ALERT_STATE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type ClusterAlertStateStrict = Exclude<ClusterAlertState, 0>;

/**
 * ClusterAlertType is the category of a cluster alert. Mirrors the backend alert types.
 *
 * Without CLUSTER_ALERT_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type ClusterAlertTypeStrict = Exclude<ClusterAlertType, 0>;

/**
 * ClusterAlertSeverity is the tier used by the UI to render badge color and announce severity to assistive tech. Color alone never conveys severity - clients must render the label.
 *
 * Without CLUSTER_ALERT_SEVERITY_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type ClusterAlertSeverityStrict = Exclude<ClusterAlertSeverity, 0>;

/** GetClusterSummaryMetricsRequest is the request for the GetClusterSummaryMetrics function */
export type GetClusterSummaryMetricsRequestStrict = Narrow<GetClusterSummaryMetricsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
}>;

/** GetClusterSummaryMetricsResponse is the response from the GetClusterSummaryMetrics function */
export type GetClusterSummaryMetricsResponseStrict = Narrow<GetClusterSummaryMetricsResponse, {
  nodes: ClusterNodeMetricsStrict[];
}>;

/**
 * GetClusterUsageMetricsRequest is the request for the GetClusterUsageMetrics function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_cluster_usage_metrics.until_gt_since]
 *     !has(this.since) || !has(this.until) || this.until > this.since
 *     message: until must be after since
 */
export type GetClusterUsageMetricsRequestStrict = Narrow<GetClusterUsageMetricsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  aggregator: AggregatorStrict;
}>;

/** GetClusterUsageMetricsResponse is the response from the GetClusterUsageMetrics function */
export type GetClusterUsageMetricsResponseStrict = Narrow<GetClusterUsageMetricsResponse, {
  cpu: MetricStrict[];
  ram: MetricStrict[];
  ramCache: MetricStrict[];
  ramRss: MetricStrict[];
  ramQdrantRss: MetricStrict[];
  disk: MetricStrict[];
  rps: MetricStrict[];
  latency: MetricStrict[];
  nodes: ClusterNodeUsageMetricsStrict[];
  gpu: MetricStrict[];
  gpuRam: MetricStrict[];
}>;

/**
 * GetClusterLogsRequest is the request for the GetClusterLogs function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_cluster_logs.until_gt_since]
 *     !has(this.since) || !has(this.until) || this.until > this.since
 *     message: until must be after since
 *   limit
 *     int32.gte = 1
 *     int32.lte = 10000
 */
export type GetClusterLogsRequestStrict = Narrow<GetClusterLogsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
}>;

/** GetClusterLogsResponse is the response from the GetClusterLogs function */
export type GetClusterLogsResponseStrict = Narrow<GetClusterLogsResponse, {
  items: LogEntryStrict[];
}>;

/**
 * GetClusterEventsRequest is the request for the GetClusterEvents function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_cluster_events.until_gt_since]
 *     !has(this.since) || !has(this.until) || this.until > this.since
 *     message: until must be after since
 */
export type GetClusterEventsRequestStrict = Narrow<GetClusterEventsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
}>;

/** GetClusterEventsRequest is the response from the GetClusterEvents function */
export type GetClusterEventsResponseStrict = Narrow<GetClusterEventsResponse, {
  items: LogEntryStrict[];
}>;

/**
 * GetClusterInferenceMetricsRequest is the request for the GetClusterInferenceMetrics function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_cluster_inference_metrics.until_gt_since]
 *     !has(this.since) || !has(this.until) || this.until > this.since
 *     message: until must be after since
 *   inference_model_id
 *     string.min_len = 1
 */
export type GetClusterInferenceMetricsRequestStrict = Narrow<GetClusterInferenceMetricsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  interval: InferenceMetricsIntervalStrict;
}>;

/** GetClusterInferenceMetricsResponse is the response from the GetClusterInferenceMetrics function */
export type GetClusterInferenceMetricsResponseStrict = Narrow<GetClusterInferenceMetricsResponse, {
  models: ClusterInferenceModelMetricsStrict[];
}>;

/**
 * ClusterInferenceModelMetrics groups histogram data for a single inference model.
 *
 * Left to runtime validation, having no type equivalent:
 *   inference_model_id
 *     string.min_len = 1
 */
export type ClusterInferenceModelMetricsStrict = Narrow<ClusterInferenceModelMetrics, {
  values: MetricStrict[];
}>;

/**
 * ClusterNodeMetrics contains metric overview for a node.
 *
 * Left to runtime validation, having no type equivalent:
 *   node_id
 *     string.min_len = 1
 */
export type ClusterNodeMetricsStrict = Require<Narrow<ClusterNodeMetrics, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  cpu: ClusterMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  ram: ClusterMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  ramCache: ClusterMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  ramRss: ClusterMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  ramQdrantRss: ClusterMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  disk: ClusterMetricOverviewStrict;
  gpu: ClusterMetricOverviewStrict;
  gpuRam: ClusterMetricOverviewStrict;
}>, "cpu" | "ram" | "ramCache" | "ramRss" | "ramQdrantRss" | "disk">;

/** ClusterMetricOverview contains both the average values of a metric over predefined intervals, and the total available value of that resource. */
export type ClusterMetricOverviewStrict = Require<Narrow<ClusterMetricOverview, {
  avg: IntervalAverageStrict[];
  /**
   * required
   *
   * Carried into the type: required.
   */
  total: NonNullable<ClusterMetricOverview["total"]>;
}>, "total">;

/**
 * IntervalAverage represents the average value of a metric over a specific time interval.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gte = 0
 */
export type IntervalAverageStrict = Require<Narrow<IntervalAverage, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  interval: NonNullable<IntervalAverage["interval"]>;
}>, "interval">;

/**
 * ResourceValue represents the total value of a resource along with its unit.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gte = 0
 *   unit
 *     string.min_len = 1
 */
export type ResourceValueStrict = ResourceValue;

/**
 * ClusterNodeUsageMetrics defines per-node usage metrics.
 *
 * Left to runtime validation, having no type equivalent:
 *   node_id
 *     string.min_len = 1
 */
export type ClusterNodeUsageMetricsStrict = Narrow<ClusterNodeUsageMetrics, {
  cpu: MetricStrict[];
  ram: MetricStrict[];
  ramCache: MetricStrict[];
  ramRss: MetricStrict[];
  ramQdrantRss: MetricStrict[];
  disk: MetricStrict[];
  gpu: MetricStrict[];
  gpuRam: MetricStrict[];
}>;

/**
 * A single metric data point.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gte = 0
 */
export type MetricStrict = Require<Narrow<Metric, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  timestamp: NonNullable<Metric["timestamp"]>;
}>, "timestamp">;

/**
 * LogEntry represents a single log line.
 *
 * Left to runtime validation, having no type equivalent:
 *   message
 *     string.min_len = 1
 */
export type LogEntryStrict = Require<Narrow<LogEntry, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  timestamp: NonNullable<LogEntry["timestamp"]>;
}>, "timestamp">;

/** ListClusterAlertsRequest is the request for the ListClusterAlerts function */
export type ListClusterAlertsRequestStrict = Narrow<ListClusterAlertsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  clusterId: Uuid;
  state: ClusterAlertStateStrict;
}>;

/** ListClusterAlertsResponse is the response from the ListClusterAlerts function */
export type ListClusterAlertsResponseStrict = Narrow<ListClusterAlertsResponse, {
  alerts: ClusterAlertStrict[];
}>;

export type ClusterAlertStrict = Narrow<ClusterAlert, {
  type: ClusterAlertTypeStrict;
  severity: ClusterAlertSeverityStrict;
  state: ClusterAlertStateStrict;
}>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterSummaryMetricsRequest, reporting GetClusterSummaryMetricsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterSummaryMetricsRequestStrictSchema =
  GetClusterSummaryMetricsRequestSchema as GenMessage<GetClusterSummaryMetricsRequest, { validType: GetClusterSummaryMetricsRequestStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterSummaryMetricsResponse, reporting GetClusterSummaryMetricsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterSummaryMetricsResponseStrictSchema =
  GetClusterSummaryMetricsResponseSchema as GenMessage<GetClusterSummaryMetricsResponse, { validType: GetClusterSummaryMetricsResponseStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterUsageMetricsRequest, reporting GetClusterUsageMetricsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterUsageMetricsRequestStrictSchema =
  GetClusterUsageMetricsRequestSchema as GenMessage<GetClusterUsageMetricsRequest, { validType: GetClusterUsageMetricsRequestStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterUsageMetricsResponse, reporting GetClusterUsageMetricsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterUsageMetricsResponseStrictSchema =
  GetClusterUsageMetricsResponseSchema as GenMessage<GetClusterUsageMetricsResponse, { validType: GetClusterUsageMetricsResponseStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterLogsRequest, reporting GetClusterLogsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterLogsRequestStrictSchema =
  GetClusterLogsRequestSchema as GenMessage<GetClusterLogsRequest, { validType: GetClusterLogsRequestStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterLogsResponse, reporting GetClusterLogsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterLogsResponseStrictSchema =
  GetClusterLogsResponseSchema as GenMessage<GetClusterLogsResponse, { validType: GetClusterLogsResponseStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterEventsRequest, reporting GetClusterEventsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterEventsRequestStrictSchema =
  GetClusterEventsRequestSchema as GenMessage<GetClusterEventsRequest, { validType: GetClusterEventsRequestStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterEventsResponse, reporting GetClusterEventsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterEventsResponseStrictSchema =
  GetClusterEventsResponseSchema as GenMessage<GetClusterEventsResponse, { validType: GetClusterEventsResponseStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterInferenceMetricsRequest, reporting GetClusterInferenceMetricsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterInferenceMetricsRequestStrictSchema =
  GetClusterInferenceMetricsRequestSchema as GenMessage<GetClusterInferenceMetricsRequest, { validType: GetClusterInferenceMetricsRequestStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.GetClusterInferenceMetricsResponse, reporting GetClusterInferenceMetricsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetClusterInferenceMetricsResponseStrictSchema =
  GetClusterInferenceMetricsResponseSchema as GenMessage<GetClusterInferenceMetricsResponse, { validType: GetClusterInferenceMetricsResponseStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ClusterInferenceModelMetrics, reporting ClusterInferenceModelMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ClusterInferenceModelMetricsStrictSchema =
  ClusterInferenceModelMetricsSchema as GenMessage<ClusterInferenceModelMetrics, { validType: ClusterInferenceModelMetricsStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ClusterNodeMetrics, reporting ClusterNodeMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ClusterNodeMetricsStrictSchema =
  ClusterNodeMetricsSchema as GenMessage<ClusterNodeMetrics, { validType: ClusterNodeMetricsStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ClusterMetricOverview, reporting ClusterMetricOverviewStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ClusterMetricOverviewStrictSchema =
  ClusterMetricOverviewSchema as GenMessage<ClusterMetricOverview, { validType: ClusterMetricOverviewStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.IntervalAverage, reporting IntervalAverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const IntervalAverageStrictSchema =
  IntervalAverageSchema as GenMessage<IntervalAverage, { validType: IntervalAverageStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ClusterNodeUsageMetrics, reporting ClusterNodeUsageMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ClusterNodeUsageMetricsStrictSchema =
  ClusterNodeUsageMetricsSchema as GenMessage<ClusterNodeUsageMetrics, { validType: ClusterNodeUsageMetricsStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.Metric, reporting MetricStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const MetricStrictSchema =
  MetricSchema as GenMessage<Metric, { validType: MetricStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.LogEntry, reporting LogEntryStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const LogEntryStrictSchema =
  LogEntrySchema as GenMessage<LogEntry, { validType: LogEntryStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ListClusterAlertsRequest, reporting ListClusterAlertsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListClusterAlertsRequestStrictSchema =
  ListClusterAlertsRequestSchema as GenMessage<ListClusterAlertsRequest, { validType: ListClusterAlertsRequestStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ListClusterAlertsResponse, reporting ListClusterAlertsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListClusterAlertsResponseStrictSchema =
  ListClusterAlertsResponseSchema as GenMessage<ListClusterAlertsResponse, { validType: ListClusterAlertsResponseStrict }>;

/**
 * Describes qdrant.cloud.monitoring.v1.ClusterAlert, reporting ClusterAlertStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ClusterAlertStrictSchema =
  ClusterAlertSchema as GenMessage<ClusterAlert, { validType: ClusterAlertStrict }>;

type MonitoringServiceStrictDescriptor = GenService<{
  /** Gets the summary metrics of a cluster in the account identified by the given ID. Required permissions: - read:clusters */
  getClusterSummaryMetrics: {
    methodKind: "unary";
    input: typeof GetClusterSummaryMetricsRequestStrictSchema;
    output: typeof GetClusterSummaryMetricsResponseStrictSchema;
  },
  /** Gets the detailed usage metrics (as timeseries) of a cluster in the account identified by the given ID. Required permissions: - read:clusters */
  getClusterUsageMetrics: {
    methodKind: "unary";
    input: typeof GetClusterUsageMetricsRequestStrictSchema;
    output: typeof GetClusterUsageMetricsResponseStrictSchema;
  },
  /** Gets the logs of a cluster in the account identified by the given ID. Required permissions: - read:clusters */
  getClusterLogs: {
    methodKind: "unary";
    input: typeof GetClusterLogsRequestStrictSchema;
    output: typeof GetClusterLogsResponseStrictSchema;
  },
  /** Gets the events of a cluster in the account identified by the given ID. This method is currently available only for clusters running in hybrid cloud environments. Required permissions: - read:clusters */
  getClusterEvents: {
    methodKind: "unary";
    input: typeof GetClusterEventsRequestStrictSchema;
    output: typeof GetClusterEventsResponseStrictSchema;
  },
  /** Gets the inference token usage metrics for a cluster. Provide `inference_model_id` to limit the response to a single model. Required permissions: - read:clusters */
  getClusterInferenceMetrics: {
    methodKind: "unary";
    input: typeof GetClusterInferenceMetricsRequestStrictSchema;
    output: typeof GetClusterInferenceMetricsResponseStrictSchema;
  },
  /** Lists the alerts for a cluster in the account identified by the given ID. Sorted by last_firing_at (most recent first). Required permissions: - read:clusters */
  listClusterAlerts: {
    methodKind: "unary";
    input: typeof ListClusterAlertsRequestStrictSchema;
    output: typeof ListClusterAlertsResponseStrictSchema;
  },
}>;

/**
 * MonitoringService provides access to monitoring data such as cluster metrics, logs, and events.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const MonitoringServiceStrict =
  MonitoringService as MonitoringServiceStrictDescriptor;

