// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/booking/v1/booking.proto

import type { Narrow, NonEmptyList, Require, Uuid } from "../../../../strict/types";
import type { AvailableAdditionalResources, AvailableStoragePerformanceTierConfigurations, GetBackupQuoteRequest, GetBackupQuoteResponse, GetGlobalQuotesRequest, GetGlobalQuotesResponse, GetPackageRequest, GetPackageResponse, GetQuoteRequest, GetQuoteResponse, GlobalPriceBreakdown, GlobalQuote, GlobalQuoteConfiguration, InferenceModel, ListGlobalPackagesRequest, ListGlobalPackagesResponse, ListInferenceModelsRequest, ListInferenceModelsResponse, ListPackagesRequest, ListPackagesResponse, ListStorageTierTypesRequest, ListStorageTierTypesResponse, ModelModality, Package, PackageStatus, PackageTier, PriceBreakdown, ResourceConfiguration, ResourceConfigurationFilter, StorageTiers, VectorType } from "./booking_pb";
import { AvailableStoragePerformanceTierConfigurationsSchema, BookingService, GetBackupQuoteRequestSchema, GetBackupQuoteResponseSchema, GetGlobalQuotesRequestSchema, GetGlobalQuotesResponseSchema, GetPackageRequestSchema, GetPackageResponseSchema, GetQuoteRequestSchema, GetQuoteResponseSchema, GlobalQuoteConfigurationSchema, InferenceModelSchema, ListGlobalPackagesRequestSchema, ListGlobalPackagesResponseSchema, ListInferenceModelsRequestSchema, ListInferenceModelsResponseSchema, ListPackagesRequestSchema, ListPackagesResponseSchema, ListStorageTierTypesRequestSchema, ListStorageTierTypesResponseSchema, PackageSchema, StorageTiersSchema } from "./booking_pb";
import type { StorageTierTypeStrict } from "../../common/v1/common.strict";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/**
 * PackageStatus defines the valid states a package can be in.
 *
 * Without PACKAGE_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type PackageStatusStrict = Exclude<PackageStatus, 0>;

/**
 * PackageTier defines the valid tiers a package can be in.
 *
 * Without PACKAGE_TIER_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type PackageTierStrict = Exclude<PackageTier, 0>;

/**
 * VectorType defines the type of vector representation.
 *
 * Without VECTOR_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type VectorTypeStrict = Exclude<VectorType, 0>;

/**
 * ModelModality defines the type of data the model processes.
 *
 * Without MODEL_MODALITY_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type ModelModalityStrict = Exclude<ModelModality, 0>;

/**
 * ListPackagesRequest is the request for the ListPackages function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[list_packages.cloud_provider_region_id_present]
 *     this.cloud_provider_id == "hybrid" || has(this.cloud_provider_region_id)
 *     message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 */
export type ListPackagesRequestStrict = Narrow<ListPackagesRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  statuses: PackageStatusStrict[];
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  tier: PackageTierStrict;
}>;

/** ListPackagesResponse is the response from the ListPackages function */
export type ListPackagesResponseStrict = Narrow<ListPackagesResponse, {
  items: PackageStrict[];
}>;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[list_global_packages.cloud_provider_region_id_present]
 *     this.cloud_provider_id == "hybrid" || has(this.cloud_provider_region_id)
 *     message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 */
export type ListGlobalPackagesRequestStrict = Narrow<ListGlobalPackagesRequest, {
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  tier: PackageTierStrict;
}>;

/** ListPackagesResponse is the response from the ListPackages function */
export type ListGlobalPackagesResponseStrict = Narrow<ListGlobalPackagesResponse, {
  items: PackageStrict[];
}>;

/**
 * GetPackageRequest is the request for the GetPackage function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[list_packages.cloud_provider_region_id_present]
 *     this.cloud_provider_id == "hybrid" || has(this.cloud_provider_region_id)
 *     message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 */
export type GetPackageRequestStrict = Narrow<GetPackageRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  id: Uuid;
}>;

/** GetPackageResponse is the response from the GetPackage function */
export type GetPackageResponseStrict = Require<Narrow<GetPackageResponse, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  package: PackageStrict;
}>, "package">;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   name
 *     string.max_len = 64
 *     string.min_len = 3
 *     string.pattern = ^[a-zA-Z0-9-_]+$
 *   type
 *     string.in = [free, paid]
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 *   unit_int_price_per_hour
 *     int32.gte = 0
 */
export type PackageStrict = Narrow<Package, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  id: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  status: PackageStatusStrict;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  tier: PackageTierStrict;
  /**
   * repeated.min_items = 1
   *
   * Carried into the type: repeated.min_items.
   */
  availableStorageTierConfigurations: NonEmptyList<AvailableStoragePerformanceTierConfigurationsStrict[]>;
}>;

/** AvailableAdditionalResources represents additional resources that can be added to the cluster. */
export type AvailableAdditionalResourcesStrict = AvailableAdditionalResources;

/** AvailableStoragePerformanceTierConfigurations represents available storage tier configurations for given package region and provider. */
export type AvailableStoragePerformanceTierConfigurationsStrict = Narrow<AvailableStoragePerformanceTierConfigurations, {
  storageTierType: StorageTierTypeStrict;
}>;

/**
 * ResourceConfiguration defines the resource configuration for a package.
 *
 * Left to runtime validation, having no type equivalent:
 *   ram
 *     string.min_len = 1
 *   cpu
 *     string.min_len = 1
 *   disk
 *     string.min_len = 1
 *   gpu
 *     string.min_len = 1
 *   gpu_ram
 *     string.min_len = 1
 */
export type ResourceConfigurationStrict = ResourceConfiguration;

/**
 * ResourceConfigurationFilter defines the resource configuration filter for listing packages.
 *
 * Left to runtime validation, having no type equivalent:
 *   ram
 *     string.min_len = 1
 *   cpu
 *     string.min_len = 1
 *   disk
 *     string.min_len = 1
 *   gpu
 *     string.min_len = 1
 */
export type ResourceConfigurationFilterStrict = ResourceConfigurationFilter;

/**
 * GetQuoteRequest is the request for the GetQuote function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_quote.cloud_provider_region_id_present]
 *     this.cloud_provider_id == "hybrid" || has(this.cloud_provider_region_id)
 *     message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 *   number_of_nodes
 *     uint32.gte = 1
 *   additional_disk_gib
 *     uint32.gte = 0
 */
export type GetQuoteRequestStrict = Narrow<GetQuoteRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  packageId: Uuid;
  storageTierType: StorageTierTypeStrict;
}>;

/**
 * PriceBreakdown represents the price breakdown for one component of a quote. Amounts are in the same currency as the enclosing GetQuoteResponse.
 *
 * Left to runtime validation, having no type equivalent:
 *   original_price_per_hour
 *     int64.gte = 0
 *   discounted_price_per_hour
 *     int64.gte = 0
 *   discount_percentage
 *     double.gte = 0
 *     double.lte = 100
 */
export type PriceBreakdownStrict = PriceBreakdown;

/**
 * GetQuoteResponse is the response from the GetQuote function
 *
 * Left to runtime validation, having no type equivalent:
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 *   original_price_per_hour
 *     int64.gte = 0
 *   discounted_price_per_hour
 *     int64.gte = 0
 *   discount_percentage
 *     double.gte = 0
 *     double.lte = 100
 */
export type GetQuoteResponseStrict = GetQuoteResponse;

export type GetGlobalQuotesRequestStrict = Narrow<GetGlobalQuotesRequest, {
  /**
   * repeated.max_items = 50
   * repeated.min_items = 1
   *
   * Carried into the type: repeated.min_items.
   */
  configurations: NonEmptyList<GlobalQuoteConfigurationStrict[]>;
}>;

/**
 * GlobalQuoteConfiguration describes a single cluster configuration to price as part of a GetGlobalQuotes request.
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[global_quote_configuration.cloud_provider_region_id_present]
 *     this.cloud_provider_id == "hybrid" || has(this.cloud_provider_region_id)
 *     message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 *   number_of_nodes
 *     uint32.gte = 1
 *   additional_disk_gib
 *     uint32.gte = 0
 */
export type GlobalQuoteConfigurationStrict = Narrow<GlobalQuoteConfiguration, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  packageId: Uuid;
  storageTierType: StorageTierTypeStrict;
}>;

/**
 * GlobalPriceBreakdown represents the list price for one component of a global quote. Amounts are in the same currency as the enclosing GlobalQuote.
 *
 * Left to runtime validation, having no type equivalent:
 *   price_per_hour
 *     int64.gte = 0
 */
export type GlobalPriceBreakdownStrict = GlobalPriceBreakdown;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 *   price_per_hour
 *     int64.gte = 0
 */
export type GlobalQuoteStrict = GlobalQuote;

/** GetGlobalQuotesResponse is the response from the GetGlobalQuotes function */
export type GetGlobalQuotesResponseStrict = GetGlobalQuotesResponse;

/**
 * GetBackupQuoteRequest is the request for the GetBackupQuote function
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_backup_quote.cloud_provider_region_id_present]
 *     this.cloud_provider_id == "hybrid" || has(this.cloud_provider_region_id)
 *     message: cloud_provider_region_id is required when cloud_provider_id is not 'hybrid'
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 *   backup_size_gib
 *     uint32.gt = 0
 */
export type GetBackupQuoteRequestStrict = Narrow<GetBackupQuoteRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/**
 * GetBackupQuoteResponse is the response from the GetBackupQuote function
 *
 * Left to runtime validation, having no type equivalent:
 *   currency
 *     string.pattern = ^[A-Z]{3}$
 *   original_price_per_hour
 *     int64.gte = 0
 *   discounted_price_per_hour
 *     int64.gte = 0
 *   discount_percentage
 *     double.gte = 0
 *     double.lte = 100
 */
export type GetBackupQuoteResponseStrict = GetBackupQuoteResponse;

/**
 * ListInferenceModelsRequest is the request for the ListInferenceModels function
 *
 * Left to runtime validation, having no type equivalent:
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 */
export type ListInferenceModelsRequestStrict = Narrow<ListInferenceModelsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListInferenceModelsResponse is the response from the ListInferenceModels function */
export type ListInferenceModelsResponseStrict = Narrow<ListInferenceModelsResponse, {
  items: InferenceModelStrict[];
}>;

/**
 * ListStorageTierTypesRequest is the request for the ListStorageTierTypes function
 *
 * Left to runtime validation, having no type equivalent:
 *   cloud_provider_id
 *     string.min_len = 3
 *   cloud_provider_region_id
 *     string.min_len = 1
 */
export type ListStorageTierTypesRequestStrict = Narrow<ListStorageTierTypesRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListStorageTierTypesResponse is the response from the ListStorageTierTypes function */
export type ListStorageTierTypesResponseStrict = Narrow<ListStorageTierTypesResponse, {
  items: StorageTiersStrict[];
}>;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   name
 *     string.min_len = 1
 *   title
 *     string.min_len = 1
 *   description
 *     string.min_len = 1
 *   vendor
 *     string.min_len = 1
 *   external_docs_url
 *     string.uri = true
 */
export type InferenceModelStrict = Narrow<InferenceModel, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  id: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  vectorType: VectorTypeStrict;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  modality: ModelModalityStrict;
}>;

/** StorageTiers holds information related to a particular storage tier */
export type StorageTiersStrict = Narrow<StorageTiers, {
  storageTierType: StorageTierTypeStrict;
}>;

/**
 * Describes qdrant.cloud.booking.v1.ListPackagesRequest, reporting ListPackagesRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListPackagesRequestStrictSchema =
  ListPackagesRequestSchema as GenMessage<ListPackagesRequest, { validType: ListPackagesRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListPackagesResponse, reporting ListPackagesResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListPackagesResponseStrictSchema =
  ListPackagesResponseSchema as GenMessage<ListPackagesResponse, { validType: ListPackagesResponseStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListGlobalPackagesRequest, reporting ListGlobalPackagesRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListGlobalPackagesRequestStrictSchema =
  ListGlobalPackagesRequestSchema as GenMessage<ListGlobalPackagesRequest, { validType: ListGlobalPackagesRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListGlobalPackagesResponse, reporting ListGlobalPackagesResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListGlobalPackagesResponseStrictSchema =
  ListGlobalPackagesResponseSchema as GenMessage<ListGlobalPackagesResponse, { validType: ListGlobalPackagesResponseStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.GetPackageRequest, reporting GetPackageRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetPackageRequestStrictSchema =
  GetPackageRequestSchema as GenMessage<GetPackageRequest, { validType: GetPackageRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.GetPackageResponse, reporting GetPackageResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetPackageResponseStrictSchema =
  GetPackageResponseSchema as GenMessage<GetPackageResponse, { validType: GetPackageResponseStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.Package, reporting PackageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const PackageStrictSchema =
  PackageSchema as GenMessage<Package, { validType: PackageStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.AvailableStoragePerformanceTierConfigurations, reporting AvailableStoragePerformanceTierConfigurationsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const AvailableStoragePerformanceTierConfigurationsStrictSchema =
  AvailableStoragePerformanceTierConfigurationsSchema as GenMessage<AvailableStoragePerformanceTierConfigurations, { validType: AvailableStoragePerformanceTierConfigurationsStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.GetQuoteRequest, reporting GetQuoteRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetQuoteRequestStrictSchema =
  GetQuoteRequestSchema as GenMessage<GetQuoteRequest, { validType: GetQuoteRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.GetGlobalQuotesRequest, reporting GetGlobalQuotesRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetGlobalQuotesRequestStrictSchema =
  GetGlobalQuotesRequestSchema as GenMessage<GetGlobalQuotesRequest, { validType: GetGlobalQuotesRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.GlobalQuoteConfiguration, reporting GlobalQuoteConfigurationStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GlobalQuoteConfigurationStrictSchema =
  GlobalQuoteConfigurationSchema as GenMessage<GlobalQuoteConfiguration, { validType: GlobalQuoteConfigurationStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.GetBackupQuoteRequest, reporting GetBackupQuoteRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetBackupQuoteRequestStrictSchema =
  GetBackupQuoteRequestSchema as GenMessage<GetBackupQuoteRequest, { validType: GetBackupQuoteRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListInferenceModelsRequest, reporting ListInferenceModelsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListInferenceModelsRequestStrictSchema =
  ListInferenceModelsRequestSchema as GenMessage<ListInferenceModelsRequest, { validType: ListInferenceModelsRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListInferenceModelsResponse, reporting ListInferenceModelsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListInferenceModelsResponseStrictSchema =
  ListInferenceModelsResponseSchema as GenMessage<ListInferenceModelsResponse, { validType: ListInferenceModelsResponseStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListStorageTierTypesRequest, reporting ListStorageTierTypesRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListStorageTierTypesRequestStrictSchema =
  ListStorageTierTypesRequestSchema as GenMessage<ListStorageTierTypesRequest, { validType: ListStorageTierTypesRequestStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.ListStorageTierTypesResponse, reporting ListStorageTierTypesResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListStorageTierTypesResponseStrictSchema =
  ListStorageTierTypesResponseSchema as GenMessage<ListStorageTierTypesResponse, { validType: ListStorageTierTypesResponseStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.InferenceModel, reporting InferenceModelStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const InferenceModelStrictSchema =
  InferenceModelSchema as GenMessage<InferenceModel, { validType: InferenceModelStrict }>;

/**
 * Describes qdrant.cloud.booking.v1.StorageTiers, reporting StorageTiersStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const StorageTiersStrictSchema =
  StorageTiersSchema as GenMessage<StorageTiers, { validType: StorageTiersStrict }>;

type BookingServiceStrictDescriptor = GenService<{
  /** Lists all packages known by the system, optional filtered. Required permissions: - None (authenticated only) */
  listPackages: {
    methodKind: "unary";
    input: typeof ListPackagesRequestStrictSchema;
    output: typeof ListPackagesResponseStrictSchema;
  },
  /** Gets the package identified by the given ID. Required permissions: - None (authenticated only) */
  getPackage: {
    methodKind: "unary";
    input: typeof GetPackageRequestStrictSchema;
    output: typeof GetPackageResponseStrictSchema;
  },
  listGlobalPackages: {
    methodKind: "unary";
    input: typeof ListGlobalPackagesRequestStrictSchema;
    output: typeof ListGlobalPackagesResponseStrictSchema;
  },
  getGlobalQuotes: {
    methodKind: "unary";
    input: typeof GetGlobalQuotesRequestStrictSchema;
    output: typeof GetGlobalQuotesResponseSchema;
  },
  /** Gets a price quote for a cluster configuration. Calculates pricing information including hourly costs, and any applicable discounts for the specified cluster configuration. Required permissions: - write:clusters */
  getQuote: {
    methodKind: "unary";
    input: typeof GetQuoteRequestStrictSchema;
    output: typeof GetQuoteResponseSchema;
  },
  /** Gets a price quote for a backup configuration. Calculates pricing information for storing a backup, based on the specified cloud provider, region, and backup size. Required permissions: - write:backups */
  getBackupQuote: {
    methodKind: "unary";
    input: typeof GetBackupQuoteRequestStrictSchema;
    output: typeof GetBackupQuoteResponseSchema;
  },
  /** Gets the list of available inference models. Required permissions: - None (authenticated only) */
  listInferenceModels: {
    methodKind: "unary";
    input: typeof ListInferenceModelsRequestStrictSchema;
    output: typeof ListInferenceModelsResponseStrictSchema;
  },
  /** Gets the list of available storage tiers for a particular region Required permissions: - None (authenticated only) */
  listStorageTierTypes: {
    methodKind: "unary";
    input: typeof ListStorageTierTypesRequestStrictSchema;
    output: typeof ListStorageTierTypesResponseStrictSchema;
  },
}>;

/**
 * BookingService is the API used to configure the booking settings (like packages objects).
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const BookingServiceStrict =
  BookingService as BookingServiceStrictDescriptor;

