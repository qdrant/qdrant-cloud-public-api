// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/quota/v1/quota.proto

import type { Narrow, Uuid } from "../../../../strict/types";
import type { GetAccountQuotasRequest, GetAccountQuotasResponse, GetAuthenticatedUserQuotasRequest, GetAuthenticatedUserQuotasResponse } from "./quota_pb";
import { GetAccountQuotasRequestSchema, GetAccountQuotasResponseSchema, GetAuthenticatedUserQuotasRequestSchema, GetAuthenticatedUserQuotasResponseSchema, QuotaService } from "./quota_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

export type GetAuthenticatedUserQuotasRequestStrict = GetAuthenticatedUserQuotasRequest;

/**
 * GetAuthenticatedUserQuotasResponse is the response from the GetAuthenticatedUserQuotas function
 *
 * Left to runtime validation, having no type equivalent:
 *   max_owned_accounts
 *     uint32.gt = 0
 *   max_member_accounts
 *     uint32.gt = 0
 */
export type GetAuthenticatedUserQuotasResponseStrict = GetAuthenticatedUserQuotasResponse;

/** GetAccountQuotasRequest is the request for the GetAccountQuotas function. */
export type GetAccountQuotasRequestStrict = Narrow<GetAccountQuotasRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/**
 * GetAccountQuotasResponse is the response from the GetAccountQuotas function.
 *
 * Left to runtime validation, having no type equivalent:
 *   max_clusters
 *     uint32.gte = 0
 *   max_cluster_nodes
 *     uint32.gte = 0
 *   max_cluster_database_api_keys
 *     uint32.gte = 0
 *   max_free_tier_clusters
 *     uint32.gte = 0
 */
export type GetAccountQuotasResponseStrict = Narrow<GetAccountQuotasResponse, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/**
 * Describes qdrant.cloud.quota.v1.GetAccountQuotasRequest, reporting GetAccountQuotasRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetAccountQuotasRequestStrictSchema =
  GetAccountQuotasRequestSchema as GenMessage<GetAccountQuotasRequest, { validType: GetAccountQuotasRequestStrict }>;

/**
 * Describes qdrant.cloud.quota.v1.GetAccountQuotasResponse, reporting GetAccountQuotasResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetAccountQuotasResponseStrictSchema =
  GetAccountQuotasResponseSchema as GenMessage<GetAccountQuotasResponse, { validType: GetAccountQuotasResponseStrict }>;

type QuotaServiceStrictDescriptor = GenService<{
  /** Get quotas for the authenticated user. Required permissions: - None (authenticated only) */
  getAuthenticatedUserQuotas: {
    methodKind: "unary";
    input: typeof GetAuthenticatedUserQuotasRequestSchema;
    output: typeof GetAuthenticatedUserQuotasResponseSchema;
  },
  /** Get quotas for the account identified by the given account ID. Required permissions: - None (authenticated only) */
  getAccountQuotas: {
    methodKind: "unary";
    input: typeof GetAccountQuotasRequestStrictSchema;
    output: typeof GetAccountQuotasResponseStrictSchema;
  },
}>;

/**
 * QuotaService is the API used to configure Quotas.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const QuotaServiceStrict =
  QuotaService as QuotaServiceStrictDescriptor;

