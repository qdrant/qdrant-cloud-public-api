// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/event/v1/events.proto

import type { Narrow, Require, Uuid } from "../../../../strict/types";
import type { Event, EventOptions, EventSource, EventType, Result, ResultStatus } from "./events_pb";
import { EventOptionsSchema, EventSchema, ResultSchema } from "./events_pb";
import type { CallerStrict } from "../../common/v1/common.strict";
import type { GenMessage } from "@bufbuild/protobuf/codegenv2";

/**
 * EventType defines the type of the event that occurred to the resource.
 *
 * Without EVENT_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type EventTypeStrict = Exclude<EventType, 0>;

/**
 * ResultStatus identifies whether an audited RPC completed successfully.
 *
 * Without RESULT_STATUS_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type ResultStatusStrict = Exclude<ResultStatus, 0>;

/**
 * EventSource identifies which API surface produced the event.
 *
 * Without EVENT_SOURCE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type EventSourceStrict = Exclude<EventSource, 0>;

/**
 * EventOptions is a custom method option to indicate that an event should be generated when this RPC is successfully called. The fields should start with 'req.' or 'resp.' to indicate whether the info needs to be extracted from the request or the response. Or 'req-md.' or 'resp-md.' to extract the info from the metadata.
 *
 * Left to runtime validation, having no type equivalent:
 *   resource_type
 *     string.min_len = 1
 *   resource_id_field
 *     string.min_len = 1
 *   resource_url_template
 *     string.min_len = 1
 *   action_type
 *     string.min_len = 1
 */
export type EventOptionsStrict = Narrow<EventOptions, {
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  eventType: EventTypeStrict;
}>;

/**
 * Result describes the outcome of the RPC that produced this event.
 *
 * Left to runtime validation, having no type equivalent:
 *   status_code
 *     int32.gte = 0
 *   error_reason
 *     string.max_len = 1024
 *     string.min_len = 1
 */
export type ResultStrict = Narrow<Result, {
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  status: ResultStatusStrict;
}>;

/**
 * Event represents a significant occurrence in the system, like a resource being created, updated, or deleted.
 *
 * Left to runtime validation, having no type equivalent:
 *   ip_address
 *     string.ipv4 = true
 *   source_rpc
 *     string.min_len = 1
 *   trace_id
 *     string.min_len = 1
 *   resource_type
 *     string.min_len = 1
 *   resource_id
 *     string.min_len = 1
 *   resource_url
 *     string.min_len = 1
 *   action_type
 *     string.min_len = 1
 */
export type EventStrict = Require<Narrow<Event, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  id: Uuid;
  result: ResultStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  caller: CallerStrict;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  source: EventSourceStrict;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  eventType: EventTypeStrict;
}>, "caller">;

/**
 * Describes qdrant.cloud.event.v1.EventOptions, reporting EventOptionsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const EventOptionsStrictSchema =
  EventOptionsSchema as GenMessage<EventOptions, { validType: EventOptionsStrict }>;

/**
 * Describes qdrant.cloud.event.v1.Result, reporting ResultStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ResultStrictSchema =
  ResultSchema as GenMessage<Result, { validType: ResultStrict }>;

/**
 * Describes qdrant.cloud.event.v1.Event, reporting EventStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const EventStrictSchema =
  EventSchema as GenMessage<Event, { validType: EventStrict }>;

