// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/serverless/platform/v1/platform.proto

import type { Narrow, Uuid } from "../../../../../strict/types";
import type { CloudProvider, CloudProviderRegion, GetCloudProviderRegionRequest, GetCloudProviderRegionResponse, GetGlobalCloudProviderRegionRequest, GetGlobalCloudProviderRegionResponse, ListCloudProviderRegionsRequest, ListCloudProviderRegionsResponse, ListCloudProvidersRequest, ListCloudProvidersResponse, ListGlobalCloudProviderRegionsRequest, ListGlobalCloudProviderRegionsResponse, ListGlobalCloudProvidersRequest, ListGlobalCloudProvidersResponse } from "./platform_pb";
import { GetCloudProviderRegionRequestSchema, GetCloudProviderRegionResponseSchema, GetGlobalCloudProviderRegionRequestSchema, GetGlobalCloudProviderRegionResponseSchema, ListCloudProviderRegionsRequestSchema, ListCloudProviderRegionsResponseSchema, ListCloudProvidersRequestSchema, ListCloudProvidersResponseSchema, ListGlobalCloudProviderRegionsRequestSchema, ListGlobalCloudProviderRegionsResponseSchema, ListGlobalCloudProvidersRequestSchema, ListGlobalCloudProvidersResponseSchema, PlatformService } from "./platform_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/** ListCloudProvidersRequest is the request for the ListCloudProviders function. */
export type ListCloudProvidersRequestStrict = Narrow<ListCloudProvidersRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListCloudProvidersResponse is the response from the ListCloudProviders function. */
export type ListCloudProvidersResponseStrict = ListCloudProvidersResponse;

export type ListGlobalCloudProvidersRequestStrict = ListGlobalCloudProvidersRequest;

/** ListGlobalCloudProvidersResponse is the response from the ListGlobalCloudProviders function. */
export type ListGlobalCloudProvidersResponseStrict = ListGlobalCloudProvidersResponse;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cloud_provider_id
 *     string.min_len = 3
 */
export type ListGlobalCloudProviderRegionsRequestStrict = ListGlobalCloudProviderRegionsRequest;

/** ListGlobalCloudProviderRegionsResponse is the response from the ListGlobalCloudProviderRegions function. */
export type ListGlobalCloudProviderRegionsResponseStrict = ListGlobalCloudProviderRegionsResponse;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   cloud_provider_id
 *     string.min_len = 3
 *   region_id
 *     string.min_len = 1
 */
export type GetGlobalCloudProviderRegionRequestStrict = GetGlobalCloudProviderRegionRequest;

/** GetGlobalCloudProviderRegionResponse is the response from the GetGlobalCloudProviderRegion function. */
export type GetGlobalCloudProviderRegionResponseStrict = GetGlobalCloudProviderRegionResponse;

/**
 * ListCloudProviderRegionsRequest is the request for the ListCloudProviderRegions function.
 *
 * Left to runtime validation, having no type equivalent:
 *   cloud_provider_id
 *     string.min_len = 3
 */
export type ListCloudProviderRegionsRequestStrict = Narrow<ListCloudProviderRegionsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** ListCloudProviderRegionsResponse is the response from the ListCloudProviderRegions function. */
export type ListCloudProviderRegionsResponseStrict = ListCloudProviderRegionsResponse;

/**
 * GetCloudProviderRegionRequest is the request for the GetCloudProviderRegion function.
 *
 * Left to runtime validation, having no type equivalent:
 *   cloud_provider_id
 *     string.min_len = 3
 *   region_id
 *     string.min_len = 1
 */
export type GetCloudProviderRegionRequestStrict = Narrow<GetCloudProviderRegionRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** GetCloudProviderRegionResponse is the response from the GetCloudProviderRegion function. */
export type GetCloudProviderRegionResponseStrict = GetCloudProviderRegionResponse;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   id
 *     string.min_len = 3
 *   name
 *     string.min_len = 1
 */
export type CloudProviderStrict = CloudProvider;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   id
 *     string.min_len = 1
 *   name
 *     string.min_len = 1
 *   provider
 *     string.min_len = 1
 *   country_iso_code
 *     string.len = 2
 *     string.pattern = ^[A-Z]{2}$
 *   geographical_sub_region
 *     string.min_len = 1
 */
export type CloudProviderRegionStrict = CloudProviderRegion;

/**
 * Describes qdrant.cloud.serverless.platform.v1.ListCloudProvidersRequest, reporting ListCloudProvidersRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListCloudProvidersRequestStrictSchema =
  ListCloudProvidersRequestSchema as GenMessage<ListCloudProvidersRequest, { validType: ListCloudProvidersRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.platform.v1.ListCloudProviderRegionsRequest, reporting ListCloudProviderRegionsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListCloudProviderRegionsRequestStrictSchema =
  ListCloudProviderRegionsRequestSchema as GenMessage<ListCloudProviderRegionsRequest, { validType: ListCloudProviderRegionsRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.platform.v1.GetCloudProviderRegionRequest, reporting GetCloudProviderRegionRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetCloudProviderRegionRequestStrictSchema =
  GetCloudProviderRegionRequestSchema as GenMessage<GetCloudProviderRegionRequest, { validType: GetCloudProviderRegionRequestStrict }>;

type PlatformServiceStrictDescriptor = GenService<{
  /** Lists all available cloud providers globally (not account-specific). Authentication is not required. */
  listGlobalCloudProviders: {
    methodKind: "unary";
    input: typeof ListGlobalCloudProvidersRequestSchema;
    output: typeof ListGlobalCloudProvidersResponseSchema;
  },
  /** Lists all cloud providers in the account identified by the given ID. Required permissions: - None (authenticated only) */
  listCloudProviders: {
    methodKind: "unary";
    input: typeof ListCloudProvidersRequestStrictSchema;
    output: typeof ListCloudProvidersResponseSchema;
  },
  /** Lists all cloud provider regions (not account-specific) identified by cloud provider ID. Authentication is not required. */
  listGlobalCloudProviderRegions: {
    methodKind: "unary";
    input: typeof ListGlobalCloudProviderRegionsRequestSchema;
    output: typeof ListGlobalCloudProviderRegionsResponseSchema;
  },
  /** Gets a specific cloud provider region (not account-specific) identified by cloud provider ID and region ID. Authentication is not required. */
  getGlobalCloudProviderRegion: {
    methodKind: "unary";
    input: typeof GetGlobalCloudProviderRegionRequestSchema;
    output: typeof GetGlobalCloudProviderRegionResponseSchema;
  },
  /** Lists all cloud provider regions in the account identified by the given ID and cloud provider. Required permissions: - None (authenticated only) */
  listCloudProviderRegions: {
    methodKind: "unary";
    input: typeof ListCloudProviderRegionsRequestStrictSchema;
    output: typeof ListCloudProviderRegionsResponseSchema;
  },
  /** Gets a specific cloud provider region in the account identified by the given ID and cloud provider. Required permissions: - None (authenticated only) */
  getCloudProviderRegion: {
    methodKind: "unary";
    input: typeof GetCloudProviderRegionRequestStrictSchema;
    output: typeof GetCloudProviderRegionResponseSchema;
  },
}>;

/**
 * PlatformService is the API used to query for cloud provider & regional information for the serverless spaces in the platform.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const PlatformServiceStrict =
  PlatformService as PlatformServiceStrictDescriptor;

