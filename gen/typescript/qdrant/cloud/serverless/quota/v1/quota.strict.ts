// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/serverless/quota/v1/quota.proto

import type { Narrow, Uuid } from "../../../../../strict/types";
import type { AccountQuota, GetQuotasRequest, GetQuotasResponse } from "./quota_pb";
import { AccountQuotaSchema, GetQuotasRequestSchema, GetQuotasResponseSchema, QuotaService } from "./quota_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/** GetQuotasRequest is the request message for the GetQuotas method. */
export type GetQuotasRequestStrict = Narrow<GetQuotasRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/**
 * AccountQuota represents the serverless platform limits and aggregated usage for an account.
 *
 * Left to runtime validation, having no type equivalent:
 *   max_spaces
 *     uint64.gte = 0
 *   max_collections_per_space
 *     uint64.gte = 0
 *   used_spaces
 *     uint64.gte = 0
 *   platform_max_size_per_collection
 *     uint64.gte = 0
 *   platform_max_workers_per_collection
 *     uint64.gte = 0
 */
export type AccountQuotaStrict = Narrow<AccountQuota, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
}>;

/** GetQuotasResponse is the response message for the GetQuotas method. */
export type GetQuotasResponseStrict = Narrow<GetQuotasResponse, {
  quota: AccountQuotaStrict;
}>;

/**
 * Describes qdrant.cloud.serverless.quota.v1.GetQuotasRequest, reporting GetQuotasRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetQuotasRequestStrictSchema =
  GetQuotasRequestSchema as GenMessage<GetQuotasRequest, { validType: GetQuotasRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.quota.v1.AccountQuota, reporting AccountQuotaStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const AccountQuotaStrictSchema =
  AccountQuotaSchema as GenMessage<AccountQuota, { validType: AccountQuotaStrict }>;

/**
 * Describes qdrant.cloud.serverless.quota.v1.GetQuotasResponse, reporting GetQuotasResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetQuotasResponseStrictSchema =
  GetQuotasResponseSchema as GenMessage<GetQuotasResponse, { validType: GetQuotasResponseStrict }>;

type QuotaServiceStrictDescriptor = GenService<{
  /** Returns account platform limits and aggregated usage across spaces. Required permissions: - read:serverless_spaces */
  getQuotas: {
    methodKind: "unary";
    input: typeof GetQuotasRequestStrictSchema;
    output: typeof GetQuotasResponseStrictSchema;
  },
}>;

/**
 * QuotaService exposes serverless **account-level** platform quotas and aggregated usage. Per-space collection and searcher caps live on Space.configuration (UpdateSpace).
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const QuotaServiceStrict =
  QuotaService as QuotaServiceStrictDescriptor;

