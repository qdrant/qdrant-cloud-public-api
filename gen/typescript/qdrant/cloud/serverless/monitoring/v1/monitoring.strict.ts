// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: qdrant/cloud/serverless/monitoring/v1/monitoring.proto

import type { Narrow, Require, Uuid } from "../../../../../strict/types";
import type { Aggregator, GetSpaceInferenceMetricsRequest, GetSpaceInferenceMetricsResponse, GetSpaceSummaryMetricsRequest, GetSpaceSummaryMetricsResponse, GetSpaceUsageMetricsRequest, GetSpaceUsageMetricsResponse, InferenceMetricsInterval, IntervalAverage, ListSpaceAlertsRequest, ListSpaceAlertsResponse, Metric, SpaceAlert, SpaceAlertSeverity, SpaceAlertState, SpaceAlertType, SpaceCollectionInferenceMetrics, SpaceCollectionMetrics, SpaceCollectionUsageMetrics, SpaceInferenceModelMetrics, SpaceMetricOverview, SpaceQuotaSnapshot } from "./monitoring_pb";
import { GetSpaceInferenceMetricsRequestSchema, GetSpaceInferenceMetricsResponseSchema, GetSpaceSummaryMetricsRequestSchema, GetSpaceSummaryMetricsResponseSchema, GetSpaceUsageMetricsRequestSchema, GetSpaceUsageMetricsResponseSchema, IntervalAverageSchema, ListSpaceAlertsRequestSchema, ListSpaceAlertsResponseSchema, MetricSchema, MonitoringService, SpaceAlertSchema, SpaceCollectionInferenceMetricsSchema, SpaceCollectionMetricsSchema, SpaceCollectionUsageMetricsSchema, SpaceInferenceModelMetricsSchema, SpaceMetricOverviewSchema } from "./monitoring_pb";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/**
 * Aggregator defines how metrics should be aggregated over time.
 *
 * Without AGGREGATOR_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type AggregatorStrict = Exclude<Aggregator, 0>;

/**
 * InferenceMetricsInterval defines the histogram bucket size for inference metrics.
 *
 * Without INFERENCE_METRICS_INTERVAL_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type InferenceMetricsIntervalStrict = Exclude<InferenceMetricsInterval, 0>;

/**
 * SpaceAlertState mirrors the backend state of a space alert. It is used both as the optional request-side filter AND echoed per row on the SpaceAlert response.
 *
 * Without SPACE_ALERT_STATE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type SpaceAlertStateStrict = Exclude<SpaceAlertState, 0>;

/**
 * SpaceAlertType is the category of a space alert. Mirrors the backend alert types.
 *
 * Without SPACE_ALERT_TYPE_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type SpaceAlertTypeStrict = Exclude<SpaceAlertType, 0>;

/**
 * SpaceAlertSeverity is the tier used by the UI to render badge color and announce severity to assistive tech. Color alone never conveys severity - clients must render the label.
 *
 * Without SPACE_ALERT_SEVERITY_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type SpaceAlertSeverityStrict = Exclude<SpaceAlertSeverity, 0>;

/**
 * GetSpaceSummaryMetricsRequest is the request for the GetSpaceSummaryMetrics function.
 *
 * Left to runtime validation, having no type equivalent:
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   collection_name_contains
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   page_size
 *     int32.gt = 0
 *     int32.lte = 250
 *   page_token
 *     string.min_len = 1
 */
export type GetSpaceSummaryMetricsRequestStrict = Narrow<GetSpaceSummaryMetricsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  spaceId: Uuid;
}>;

/**
 * GetSpaceSummaryMetricsResponse is the response from the GetSpaceSummaryMetrics function.
 *
 * Left to runtime validation, having no type equivalent:
 *   total_size
 *     int32.gte = 0
 *   next_page_token
 *     string.min_len = 1
 */
export type GetSpaceSummaryMetricsResponseStrict = Require<Narrow<GetSpaceSummaryMetricsResponse, {
  items: SpaceCollectionMetricsStrict[];
  /**
   * required
   *
   * Carried into the type: required.
   */
  quota: NonNullable<GetSpaceSummaryMetricsResponse["quota"]>;
}>, "quota">;

/**
 * GetSpaceUsageMetricsRequest is the request for the GetSpaceUsageMetrics function.
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_space_usage_metrics.until_gt_since]
 *     !has(this.since) || !has(this.until) || this.until > this.since
 *     message: until must be after since
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   collection_name_contains
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   page_size
 *     int32.gt = 0
 *     int32.lte = 250
 *   page_token
 *     string.min_len = 1
 */
export type GetSpaceUsageMetricsRequestStrict = Narrow<GetSpaceUsageMetricsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  spaceId: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  aggregator: AggregatorStrict;
}>;

/**
 * GetSpaceUsageMetricsResponse is the response from the GetSpaceUsageMetrics function.
 *
 * Left to runtime validation, having no type equivalent:
 *   total_size
 *     int32.gte = 0
 *   next_page_token
 *     string.min_len = 1
 */
export type GetSpaceUsageMetricsResponseStrict = Require<Narrow<GetSpaceUsageMetricsResponse, {
  items: SpaceCollectionUsageMetricsStrict[];
  /**
   * required
   *
   * Carried into the type: required.
   */
  quota: NonNullable<GetSpaceUsageMetricsResponse["quota"]>;
}>, "quota">;

/**
 * GetSpaceInferenceMetricsRequest is the request for the GetSpaceInferenceMetrics function.
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[get_space_inference_metrics.until_gt_since]
 *     !has(this.since) || !has(this.until) || this.until > this.since
 *     message: until must be after since
 *   inference_model_id
 *     string.min_len = 1
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   collection_name_contains
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   page_size
 *     int32.gt = 0
 *     int32.lte = 250
 *   page_token
 *     string.min_len = 1
 */
export type GetSpaceInferenceMetricsRequestStrict = Narrow<GetSpaceInferenceMetricsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  spaceId: Uuid;
  /**
   * enum.defined_only = true
   * enum.not_in = [0]
   *
   * Carried into the type: enum.not_in.
   */
  interval: InferenceMetricsIntervalStrict;
}>;

/**
 * GetSpaceInferenceMetricsResponse is the response from the GetSpaceInferenceMetrics function.
 *
 * Left to runtime validation, having no type equivalent:
 *   total_size
 *     int32.gte = 0
 *   next_page_token
 *     string.min_len = 1
 */
export type GetSpaceInferenceMetricsResponseStrict = Narrow<GetSpaceInferenceMetricsResponse, {
  items: SpaceCollectionInferenceMetricsStrict[];
}>;

/**
 * SpaceCollectionInferenceMetrics groups inference metrics for a single collection.
 *
 * Left to runtime validation, having no type equivalent:
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 */
export type SpaceCollectionInferenceMetricsStrict = Narrow<SpaceCollectionInferenceMetrics, {
  models: SpaceInferenceModelMetricsStrict[];
}>;

/**
 * SpaceInferenceModelMetrics groups histogram data for a single inference model.
 *
 * Left to runtime validation, having no type equivalent:
 *   inference_model_id
 *     string.min_len = 1
 */
export type SpaceInferenceModelMetricsStrict = Narrow<SpaceInferenceModelMetrics, {
  values: MetricStrict[];
}>;

/**
 * ListSpaceAlertsRequest is the request for the ListSpaceAlerts function.
 *
 * Left to runtime validation, having no type equivalent:
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   collection_name_contains
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   space_global_only
 *     bool.const = true
 *   page_size
 *     int32.gt = 0
 *     int32.lte = 250
 *   page_token
 *     string.min_len = 1
 */
export type ListSpaceAlertsRequestStrict = Narrow<ListSpaceAlertsRequest, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  accountId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  spaceId: Uuid;
  state: SpaceAlertStateStrict;
}>;

/**
 * ListSpaceAlertsResponse is the response from the ListSpaceAlerts function.
 *
 * Left to runtime validation, having no type equivalent:
 *   total_size
 *     int32.gte = 0
 *   next_page_token
 *     string.min_len = 1
 */
export type ListSpaceAlertsResponseStrict = Narrow<ListSpaceAlertsResponse, {
  items: SpaceAlertStrict[];
}>;

/**
 *
 * Left to runtime validation, having no type equivalent:
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 */
export type SpaceAlertStrict = Narrow<SpaceAlert, {
  type: SpaceAlertTypeStrict;
  severity: SpaceAlertSeverityStrict;
  state: SpaceAlertStateStrict;
}>;

/**
 * SpaceQuotaSnapshot holds space-scoped effective quota ceilings and current collection count. A value of 0 on a ceiling field means unlimited for that dimension (same semantics as serverless-quota-sidecar GetSpaceLimits).
 *
 * Left to runtime validation, having no type equivalent:
 *   max_collections
 *     uint64.gte = 0
 *   current_collections
 *     uint64.gte = 0
 *   effective_max_size_per_collection_bytes
 *     uint64.gte = 0
 *   searcher_effective_max_workers
 *     uint64.gte = 0
 */
export type SpaceQuotaSnapshotStrict = SpaceQuotaSnapshot;

/**
 * SpaceCollectionMetrics contains a metric overview for a single collection.
 *
 * Left to runtime validation, having no type equivalent:
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 *   point_count
 *     uint64.gte = 0
 *   used_storage_bytes
 *     uint64.gte = 0
 *   current_searcher_workers
 *     uint64.gte = 0
 */
export type SpaceCollectionMetricsStrict = Require<Narrow<SpaceCollectionMetrics, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  searchRequests: SpaceMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  writeRequests: SpaceMetricOverviewStrict;
  /**
   * required
   *
   * Carried into the type: required.
   */
  searchLatency: SpaceMetricOverviewStrict;
}>, "searchRequests" | "writeRequests" | "searchLatency">;

/** SpaceMetricOverview contains average values of a metric over predefined intervals. */
export type SpaceMetricOverviewStrict = Narrow<SpaceMetricOverview, {
  avg: IntervalAverageStrict[];
}>;

/**
 * IntervalAverage represents the average value of a metric over a specific time interval.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gte = 0
 */
export type IntervalAverageStrict = Require<Narrow<IntervalAverage, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  interval: NonNullable<IntervalAverage["interval"]>;
}>, "interval">;

/**
 * SpaceCollectionUsageMetrics defines per-collection usage metrics as timeseries.
 *
 * Left to runtime validation, having no type equivalent:
 *   collection_name
 *     string.max_len = 255
 *     string.min_len = 1
 *     string.pattern = ^[a-zA-Z0-9-_.]+$
 */
export type SpaceCollectionUsageMetricsStrict = Narrow<SpaceCollectionUsageMetrics, {
  searchRequests: MetricStrict[];
  writeRequests: MetricStrict[];
  searchLatency: MetricStrict[];
  pointCount: MetricStrict[];
  usedStorageBytes: MetricStrict[];
  currentSearcherWorkers: MetricStrict[];
}>;

/**
 * A single metric data point.
 *
 * Left to runtime validation, having no type equivalent:
 *   value
 *     double.gte = 0
 */
export type MetricStrict = Require<Narrow<Metric, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  timestamp: NonNullable<Metric["timestamp"]>;
}>, "timestamp">;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.GetSpaceSummaryMetricsRequest, reporting GetSpaceSummaryMetricsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetSpaceSummaryMetricsRequestStrictSchema =
  GetSpaceSummaryMetricsRequestSchema as GenMessage<GetSpaceSummaryMetricsRequest, { validType: GetSpaceSummaryMetricsRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.GetSpaceSummaryMetricsResponse, reporting GetSpaceSummaryMetricsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetSpaceSummaryMetricsResponseStrictSchema =
  GetSpaceSummaryMetricsResponseSchema as GenMessage<GetSpaceSummaryMetricsResponse, { validType: GetSpaceSummaryMetricsResponseStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.GetSpaceUsageMetricsRequest, reporting GetSpaceUsageMetricsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetSpaceUsageMetricsRequestStrictSchema =
  GetSpaceUsageMetricsRequestSchema as GenMessage<GetSpaceUsageMetricsRequest, { validType: GetSpaceUsageMetricsRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.GetSpaceUsageMetricsResponse, reporting GetSpaceUsageMetricsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetSpaceUsageMetricsResponseStrictSchema =
  GetSpaceUsageMetricsResponseSchema as GenMessage<GetSpaceUsageMetricsResponse, { validType: GetSpaceUsageMetricsResponseStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.GetSpaceInferenceMetricsRequest, reporting GetSpaceInferenceMetricsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetSpaceInferenceMetricsRequestStrictSchema =
  GetSpaceInferenceMetricsRequestSchema as GenMessage<GetSpaceInferenceMetricsRequest, { validType: GetSpaceInferenceMetricsRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.GetSpaceInferenceMetricsResponse, reporting GetSpaceInferenceMetricsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const GetSpaceInferenceMetricsResponseStrictSchema =
  GetSpaceInferenceMetricsResponseSchema as GenMessage<GetSpaceInferenceMetricsResponse, { validType: GetSpaceInferenceMetricsResponseStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.SpaceCollectionInferenceMetrics, reporting SpaceCollectionInferenceMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SpaceCollectionInferenceMetricsStrictSchema =
  SpaceCollectionInferenceMetricsSchema as GenMessage<SpaceCollectionInferenceMetrics, { validType: SpaceCollectionInferenceMetricsStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.SpaceInferenceModelMetrics, reporting SpaceInferenceModelMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SpaceInferenceModelMetricsStrictSchema =
  SpaceInferenceModelMetricsSchema as GenMessage<SpaceInferenceModelMetrics, { validType: SpaceInferenceModelMetricsStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.ListSpaceAlertsRequest, reporting ListSpaceAlertsRequestStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListSpaceAlertsRequestStrictSchema =
  ListSpaceAlertsRequestSchema as GenMessage<ListSpaceAlertsRequest, { validType: ListSpaceAlertsRequestStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.ListSpaceAlertsResponse, reporting ListSpaceAlertsResponseStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ListSpaceAlertsResponseStrictSchema =
  ListSpaceAlertsResponseSchema as GenMessage<ListSpaceAlertsResponse, { validType: ListSpaceAlertsResponseStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.SpaceAlert, reporting SpaceAlertStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SpaceAlertStrictSchema =
  SpaceAlertSchema as GenMessage<SpaceAlert, { validType: SpaceAlertStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.SpaceCollectionMetrics, reporting SpaceCollectionMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SpaceCollectionMetricsStrictSchema =
  SpaceCollectionMetricsSchema as GenMessage<SpaceCollectionMetrics, { validType: SpaceCollectionMetricsStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.SpaceMetricOverview, reporting SpaceMetricOverviewStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SpaceMetricOverviewStrictSchema =
  SpaceMetricOverviewSchema as GenMessage<SpaceMetricOverview, { validType: SpaceMetricOverviewStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.IntervalAverage, reporting IntervalAverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const IntervalAverageStrictSchema =
  IntervalAverageSchema as GenMessage<IntervalAverage, { validType: IntervalAverageStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.SpaceCollectionUsageMetrics, reporting SpaceCollectionUsageMetricsStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const SpaceCollectionUsageMetricsStrictSchema =
  SpaceCollectionUsageMetricsSchema as GenMessage<SpaceCollectionUsageMetrics, { validType: SpaceCollectionUsageMetricsStrict }>;

/**
 * Describes qdrant.cloud.serverless.monitoring.v1.Metric, reporting MetricStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const MetricStrictSchema =
  MetricSchema as GenMessage<Metric, { validType: MetricStrict }>;

type MonitoringServiceStrictDescriptor = GenService<{
  /** Gets the summary metrics of a space in the account identified by the given ID. Metrics are aggregated per collection and may be paginated (a space can have many collections). The response also includes space-level effective quotas and current collection count. Required permissions: - read:serverless_spaces */
  getSpaceSummaryMetrics: {
    methodKind: "unary";
    input: typeof GetSpaceSummaryMetricsRequestStrictSchema;
    output: typeof GetSpaceSummaryMetricsResponseStrictSchema;
  },
  /** Gets the detailed usage metrics (as timeseries) of a space in the account identified by the given ID. Metrics are aggregated per collection and may be paginated (a space can have many collections). Required permissions: - read:serverless_spaces */
  getSpaceUsageMetrics: {
    methodKind: "unary";
    input: typeof GetSpaceUsageMetricsRequestStrictSchema;
    output: typeof GetSpaceUsageMetricsResponseStrictSchema;
  },
  /** Gets the inference token usage metrics for a space. Metrics are aggregated per collection and may be paginated (a space can have many collections). Provide `inference_model_id` to limit the response to a single model. Required permissions: - read:serverless_spaces */
  getSpaceInferenceMetrics: {
    methodKind: "unary";
    input: typeof GetSpaceInferenceMetricsRequestStrictSchema;
    output: typeof GetSpaceInferenceMetricsResponseStrictSchema;
  },
  /** Lists the alerts for a space in the account identified by the given ID. Alerts may be space-global or scoped to a collection, sorted by last_firing_at (most recent first), and may be paginated. Required permissions: - read:serverless_spaces */
  listSpaceAlerts: {
    methodKind: "unary";
    input: typeof ListSpaceAlertsRequestStrictSchema;
    output: typeof ListSpaceAlertsResponseStrictSchema;
  },
}>;

/**
 * MonitoringService provides access to monitoring data for serverless spaces, such as per-collection metrics, inference token usage, and alerts. Unlike cluster monitoring, this service does not expose logs or events.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const MonitoringServiceStrict =
  MonitoringService as MonitoringServiceStrictDescriptor;

